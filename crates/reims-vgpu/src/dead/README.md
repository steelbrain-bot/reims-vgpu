# `dead/` — disconnected legacy source

This directory is **not a module.** No `mod` declaration reaches it, nothing here
compiles, nothing here is feature-gated, and nothing here is linkable. It is
source to read.

## Why it exists

`AGENTS.md` and the replacement plan's Seam 6 say a group's legacy counterpart is
*deleted* in the commit that wires the group. The project owner amended that to
**disconnect instead of delete**, for one reason: when a live boot regresses on a
packet class that has just moved, the question is "what did the old arm do", and
`git log` over a 400k-line tree is a poor way to ask it. Unreachability is what
the prohibition on two semantic models actually needs, and removing the `mod`
declaration gives that by construction rather than by a flag.

## The rules, and they are not negotiable

- **Nothing is resurrected from here.** Not by a `mod`, not by a feature, not by
  a copy-paste. A regression is read here and *fixed in the new owner*.
- **No build-time or run-time switch may reach `dead/`.** A `#[cfg]` that turns a
  file here back on is the second semantic model the plan forbids, wearing a
  different hat.
- **Every move appends a row below**, naming what moved, the commit that
  replaced it, and the owner-level tests that replaced the legacy tests moving
  with it. Those tests stop running the moment they move; a group that ships
  without replacement coverage has silently lost it, and the row is where that
  is checked.
- **A coverage name is written ``fn `name` ``.** The `fn` is not decoration: it
  is what separates a promise about a function from the prose around it, so a
  row can go on naming a directory, a route family or a slug that no longer
  exists in the same sentence. The gate below reads exactly those names and
  requires each to be a function that still exists — before this marker existed
  it read nine coverage claims and could verify two, while its own doc and the
  rule above both said it verified all nine.
- **The four rules above are a test, not a convention.**
  `crates/reims-vgpu/tests/dead_is_unreachable_by_construction.rs` refuses a
  `mod dead` declaration, a `#[path]` attribute pointing into a `dead/`
  directory, an `include!` splicing a `dead/` file, a Cargo target whose `path`
  points into one, any path naming `dead` from live source, and a `dead/` with
  no register — and it checks that every function a register row *declares* as
  replacement coverage is still a function, because that row is the only record
  that a moved group's coverage survived and nothing in the build relates the
  two. It reads both tables: seven of the nine coverage claims in this file are
  in the three-column live-validation table, and reading only the five-column
  register was reading the smaller half. Six doors, because the module tree is not the only one: a
  `[[test]]` with `path = "src/dead/…"` compiles and links the file as its own
  crate root while a walk of `src/` reports the tree clean. A convention with no gate is what the amendment was written
  against: `mod dead;` compiles, and the second semantic model it re-links
  shows up as a behaviour difference on a live boot weeks later, attributed to
  whatever else changed that week.
- **`dead/` is deleted wholesale, in one commit,** once every group has moved and
  the Seam 6 gates are green. It is not pruned incrementally — a half-emptied
  `dead/` reads as "these were the ones worth keeping", which is the opposite of
  what it means.

## What is still live, read off the tree rather than off the plan

The plan's "final group" section (2026-09-02) names that group as `Namespace` +
`RefResolver` + the four `ingress::Gap` classes + the ingress switch. **Three of
those four have since moved, and the section has not been rewritten**, so a
reader taking it as current would go looking for work that is done. As of
`02bd23cd`:

- **`Namespace` and `RefResolver` are the production authority.**
  `DeviceState::object_name` asks `reims_vgpu_core::resolve::TaskNamespaces` on
  the owner it holds, and `declare_object`, `retire_object_name`,
  `define_task_namespace`, `delete_task_namespace`, `apply_lifetime` and the
  access source all route through `reims_vgpu_core::lifecycle::Lifecycle`, which
  is where `Namespace` lives. There is no second name table.
- **The ingress switch is cut.** Every accepted packet reaches
  `runtime::ingress::device_packet` through `admit_and_park`, which is what
  advances the head.
- **`ingress::Gap` is down from four variants to two** — `Unresolved` and
  `ExecStreamsUnread` — and `packet_class_unclassified` is zero on macos-15 and
  macos-26 both, so the first of those refuses nothing a driven guest sends.

What blocks the wholesale delete is the third item on Seam 6's own disconnect
list, and it is one thing rather than a list: **the process-global mutable
engine.** `backend/vulkan/engine` is 62 000 lines behind
`static ENGINE: Lazy<Mutex<EngineState>>` plus nine more process-global statics
beside it. The second item this paragraph used to name, `BLIT_FAIL_REASON` — a
thread-local carrying a reason the observation channel should carry — is gone:
`BlitFailure` and `BlitStatus::Failed { failure, reason }` replaced it in
`13f9aa49`, and `br` is the only constructor, so a refusal without a reason is
now unspellable.

Two of Seam 6's structural zeros that were *not* about the engine have since
closed, and neither was where a reader would have looked for them. "No host
thread is created per EXEC, command buffer, draw, **save**, or watchdog
operation" was violated twice over: arming the compute stall proxy spawned a
thread per dispatch that slept the whole threshold holding a copy of the
kernel's SPIR-V (`5c1be382` — one registry, one thread, sixteen slots), and
persisting the pipeline cache spawned a thread per pipeline creation
(`b0bb5b8a` — the owner the plan's own pipeline section asks for). Both are now
gated behind `/proc/self/task` counts taken *after* the single owner is up, so
the gate reads per-operation rather than per-process. What is left of the thread
survey is one long-lived worker each for m2v translation, completion stamping
and queue ownership, and the plan says a dedicated owner thread is not itself a
defect.

That is also what holds the rest of `reims-vgpu-vulkan` out. Twelve of its
thirty-six modules are wired — `raster`, `swapchain`, `vertex`, `blend`,
`depth_stencil`, `topology`, `sampler`, `pixel`, `view`, `memory`, `queues`,
`buffer` — and every one of them is a pure translation whose caller kept its
ownership. The next tier is not: `resident` and `placement` own native handles
and retirement points, `transfer`, `record`, `pass`, `renderpass` and `barrier`
are written against them, and the plan's own ordering rule says a module owning
handles, caches or submission lifetimes wires once the model owning those
lifetimes is in place. The engine is that model, and it has not moved.

## Register

Rows are ordered as they landed. A row's **step** is either a *wiring step*
(`W`n) — a pure translation or plan module whose legacy caller keeps its
ownership and loses only its duplicate arithmetic, per the plan's 2026-09-01
amendment — or a *group* (`G`n), a set of packet classes moving to
`DeviceTransaction` together under the 2026-09-02 decomposition amendment. The
two are not the same claim and are not recorded as if they were: a wiring step
moves a decision, a group moves a lifetime.

| Step | Moved | From | Replaced by | Owner-level tests that replaced the moved ones |
|---|---|---|---|---|
| W1 | `child_deprecated_ops.rs` — `CHILD_DEPRECATED_OPS`, `is_deprecated_child_opcode`, and the ledger test that walked them | `model/regs.rs`, `runtime/decode/ledger.rs` | `reims_vgpu_core::control::ControlKind::of` ⇒ `RetiredSlot`, read through `runtime::drain::is_retired_control_slot`. **What moved is the classification, not the envelope**: the drain still owns the stamp waits, the ordering position and the completion word for these packets, so this is a wiring step and not the inert class's cutover. | `runtime::decode::ledger::packets::the_ledger_judges_a_retired_set_at_all` (new — asserts the set's size against the reference host rather than against a transcription of the ledger); `runtime::decode::ledger::packets::no_live_command_is_also_a_retired_slot` and `no_ledger_row_names_a_command_this_device_cannot_receive` (re-sourced off the ledger, replacing the `const` assertion in `model::regs` that went with the table); `runtime::drain::tests::a_retired_slot_is_reported_as_retired_and_not_as_undecodable` (re-sourced off the ledger, so it now covers every judged slot rather than every transcribed one) |
| W2 | *(nothing moved)* | `runtime/drain/mod.rs` — the inline `if opcode == TAHOE { WithKeyLimit } else { WithoutKeyLimit }`, and the two arms that each named their own request decoder | `reims_vgpu_core::query::QueryKind::of` + `query::request_words`, joined by `runtime::drain::query_request` | No row content moves: the legacy counterpart was an expression inside a live match arm, not a file, so there is nothing `dead/` can hold. Recorded anyway so the register is the list of wiring steps and not only the list of files. New coverage: `runtime::decode::ledger::packets::the_query_arms_and_the_models_questions_are_the_same_four` — the check the drain cannot make for itself, since it sees one opcode at a time. |
| W3 | *(nothing moved)* | `runtime/drain/mod.rs` — the inline `packet.opcode == ROOT_OP_DEFINE_FIFO` that decided which end of a channel lifetime a packet was, and the silent `Ok(_) => {}` beside the range gate | `reims_vgpu_core::control::resolve` ⇒ `ControlOp::Channel { transition, domain }` | Again an expression rather than a file. New coverage: `runtime::drain::tests::a_channel_lifetime_outside_the_channel_range_is_reported` — the drop the old arm made in silence. |
| W4 | `event_decoder.rs` — all of `runtime/decode/event.rs` (the decoder, its `Command`/`Kind`/`DecodeStatus`, the refused-opcode list and its five tests), plus `runtime::fence_exec::tests::event_wait_timeout_unsupported` and the ledger test that drove it | `runtime/decode/event.rs` (whole file), `runtime/decode/mod.rs` (`pub mod event;`), `runtime/fence_exec.rs`, `runtime/decode/ledger.rs`, `runtime/plan/event_sync.rs` | `reims_vgpu_protocol::decode::sync::decode` for the lift, called from `runtime::exec::handle_event_record`; `reims_vgpu_protocol::sync::EventKind` for the two kinds the wire has. `plan_event` lost its `has_timeout` parameter and `Decision`/`Reason` lost their `WaitTimeoutUnsupported` variants — the settled row is refused where the ledger settled it, not a second time in the planner. Still a wiring step and not a group: `execute_event` keeps the fence-generation store and `core::resolve::event` is **not** wired, because it resolves an event ref through a namespace this device does not hold events in. | `runtime::exec::tests::a_bounded_event_wait_is_refused_by_contract_and_leaves_the_generation_alone` (new — the bounded wait's refusal reaches the failure channel from the segment walk, under the row's own name, and the signal ahead of it still lands); `runtime::decode::ledger::the_event_rail_lifts_its_records_and_refuses_its_one_settled_row` (replaces the per-rail decoder cross-check, and adds the claim the old one could not make: exactly one event row is `RefusedByContract` rather than `Unjudged`); `golden_vectors::event_signal_golden` (same bytes, re-pointed at the decoder in production) |
| W5 | `stream_framer.rs` — all of `runtime/decode/stream.rs` (segment and record framing, `Segment`, `Record`, `DecodeStatus`'s twenty-odd checks, `segment_disposition`, `segment_index_for_offset`, `SEGMENT_CHAIN_ROUTES`), plus the two `runtime::exec::tests` cases whose subjects it constructed | `runtime/decode/stream.rs` (whole file), `runtime/decode/mod.rs` (`pub mod stream;`), `runtime/exec/mod.rs`, `runtime/exec/vulkan.rs`, `runtime/exec/tests.rs`, `tests/golden_vectors.rs` | `reims_vgpu_protocol::segment::SegmentStream` for segments and `reims_vgpu_wire::op::OpStream` for the records inside one. `FramedSegment` carries the command window as a slice, so the sixteen `stream_reval_*` checks are gone with the state they guarded rather than moved; the per-segment index is counted instead of found by a quadratic re-walk; `segment_chain_route` moved to `runtime::exec` and now takes a `SegmentLifetime`, which also fixed a census that counted every segment three times on the Vulkan rail. **Carries one guest-visible behaviour change** — an unknown segment type ends the walk instead of being stepped over; see the header of `stream_framer.rs`. | `runtime::exec::tests::a_record_overrunning_its_segment_names_the_check_rather_than_ending_quietly` (new — the record-length half of the old defect, which is the half still expressible, and it asserts the well-formed record ahead of it still ran); `runtime::exec::tests::an_unknown_segment_family_ends_the_walk_and_the_envelope_does_not` (new — pins the behaviour change end to end: segments before the unknown family execute, the ones after do not, the line names the type and the count, and the protection envelope is still skipped in silence); `walking_a_well_formed_segment_to_its_end_logs_nothing` and `a_stream_that_will_not_frame_says_so_instead_of_executing_nothing` (kept, re-pointed at the new framing and its refusal names); `golden_vectors::stream_segment_record_roundtrip_shape` (same bytes, re-pointed) |
| W6 | *(nothing moved yet — the legacy blit decoder shrank rather than left)* | `runtime/exec/mod.rs` (`handle_blit_record`'s fence, ICB-optimize and content-representation arms), `runtime/blit_exec/mod.rs` (`execute_blit_fence`'s two refusals) | `reims_vgpu_core::operation::classify` decides the record's class from the ledger row; `reims_vgpu_protocol::decode::{sync, icb, resource_state}` lift the three settled classes. `execute_blit_fence` takes a `FenceRecord`, so `fence_wrong_kind` and `fence_bad_opcode` are unrepresentable. **The rows the ledger has not settled deliberately keep this device's own decoder**, because their decline census is the evidence that will settle them — see `handle_blit_device_decoded_record`. `runtime/decode/blit.rs` cannot move to `dead/` until the transfers move, which is the next step on this rail. | `runtime::exec::tests::each_icb_blit_record_reaches_a_counter_that_names_which_one_it_is` and `each_blit_spi_record_reaches_a_counter_that_names_which_one_it_is` (kept, and now also prove the settled/unsettled split routes correctly); `runtime::blit_exec::tests::blit_fence_{update_then_wait,wait_pending_without_update,zero_ref_fails}` (re-pointed at `FenceRecord`) |
| W7 | `blit_decoder.rs` — all of `runtime/decode/blit.rs`: the flat `Command` with fifty-odd fields, `Kind`, `CopyKind`, `RefKind`, `FillSource`, `parse_blit_options`/`BlitOptionError`, and its seventeen tests | `runtime/decode/blit.rs` (whole file), `runtime/decode/mod.rs` (`pub mod blit;`), `runtime/blit_exec/{mod,vulkan,tests}.rs`, `runtime/exec/mod.rs`, `runtime/decode/ledger.rs`, `backend/{mod,vulkan/mod}.rs`, `tests/{golden_vectors,wire_fixtures_reach_the_decoders}.rs` | `reims_vgpu_protocol::decode::blit::BlitRecord` for the nine transfers, whose variants now carry **named payloads** so each executor arm takes the one record it handles by reference — `execute_blit` matches the enum and the seven `exec_*` arms are functions of `BufferToBuffer`, `BufferToTexture`, `TextureToBuffer`, `TextureRegion`, `TextureSlices` and `FillBuffer`. A record charged to one family and executed by another is no longer expressible: the census route and the arm both come from `BlitRecord::kind()`, where they used to be two reads of a `kind` tag and a `copy_kind` tag a decoder set separately. `has_options` is gone with `parse_blit_options` — a record with no `options:` word selects the whole texel, which is what `protocol::blit::select_aspect(0)` answers, so "no options word" and "options word is zero" stopped being two states. `generateMipmapsForTexture:` is answered by the caller, since `runtime::mipmap` owns the chain. **The four rows the ledger has not settled did not move**: `runtime/decode/blit_spi.rs` is new and decodes exactly them — the indirect-command reset and copy and the two texture fills — because their decline census is the evidence that will settle them, and it refuses every settled opcode by name so no record has two readings. | `reims_vgpu_protocol::decode::blit::{a_texture_to_buffer_copy_reads_no_byte_past_its_options, a_region_copy_with_options_is_refused_at_the_plain_forms_length, a_slice_and_level_copy_lifts_its_six_counts_in_the_records_order}` (new — the three field-order and length claims that moved with `decode_b2b`, `decode_copy_slice_level_0x13e` and `texture_to_texture_options_len`); `reims_vgpu_protocol::blit::an_option_word_selects_one_plane_or_names_why_it_cannot` (new — replaces `parse_blit_options_aspects` at the owner, and adds the claim the old one could not make: the three refusals do not share a slug); `runtime::decode::blit_spi::{both_texture_fills_read_their_extent_from_the_size_and_not_the_origin, the_two_icb_mutations_are_told_apart_by_whether_they_name_a_destination, the_two_texture_fills_keep_their_sources_apart, the_settled_optimize_hint_is_not_a_record_this_decoder_owns, no_transfer_opcode_is_a_record_this_decoder_owns, a_record_short_of_its_body_is_refused}` (new — replace `both_texture_fills_decode_their_region_size_before_origin`, `an_icb_blit_record_is_read_rather_than_refused_whole` and `each_spi_record_refuses_a_length_one_byte_short`, and the last three are the claim that keeps `dead/`'s prohibition structural: no settled row is decodable twice); `runtime::decode::ledger::every_blit_row_reaches_exactly_one_decoder_and_the_ledger_picks_it` (new — replaces `the_blit_decoder_recognises_every_blit_operation_the_ledger_records` and `the_blit_opcode_table_is_exactly_apples_blit_manifest`, and is strictly stronger: the rail has five decoders now, so "no row is dropped by all of them" and "no row is claimed by two" are both asserted where one decoder could only assert the first); `golden_vectors::stream_segment_record_roundtrip_shape` and `corpus_property_random_decode_no_panic` (same bytes, re-pointed at the lift in production); the ~40 `runtime::blit_exec::tests` executor cases (kept, re-pointed at the payload structs — `copy_cmd(CopyKind::…)` became one builder per record, so a test can no longer construct a copy whose `kind` and `copy_kind` disagree) |
| W8 | *(nothing moved yet — the legacy compute decoder shrank rather than left)* | `runtime/exec/mod.rs` (`handle_compute_record`'s entry), `runtime/compute_exec/mod.rs` (`apply_record_inner`'s five no-op arms, `note_residency_declaration` and `ComputeResidencyDeclared`) | `reims_vgpu_core::operation::classify` decides the record's class from the ledger row, and the three classes that own no segment state are answered from it: `reims_vgpu_protocol::decode::sync` lifts the two barriers, `decode::resource_state` lifts the compressed-reinterpretation flush, and `decode::residency::lift` lifts the two unqualified residency declarations — the first device call into that module at all, which until now was reachable from nothing. **The disjointness is the reason this group is separable and it is structural**: none of the five records touches `seg.acc`, `seg.session` or `seg.block`, and the test asserts that rather than arguing it. A heap declaration carrying no usage is now the record's own shape (`ResidencyRecord::usage: Option<_>`) instead of a zero the decoder filled in, so "declared nothing" and "declared no usage" stopped being one word. **The rail's other eleven unsettled rows did not move**: the fence pair (`0xd4`/`0xd5`), the seven control-flow records and the two indirect-command executions are all `Unresolved`, so `classify` returns `None` for them and the protocol crate will not lift a row it has not settled — and unlike the blit rail's unsettled four, these feed live executors (`fence_exec::execute_fence`, `MetalSession::{encode_control,encode_icb}`) rather than declines, so they keep this device's decoder until the record types their executors take can change with them. `runtime/decode/compute.rs` cannot move to `dead/` until that happens. | `runtime::exec::tests::each_ledger_routed_compute_class_reaches_a_counter_that_names_which_one_it_is` (new — the five records, each reaching exactly one of five counters and none of the other four, plus the claim that makes the group a group: no segment state moved); `runtime::exec::tests::a_ledger_routed_compute_record_short_of_its_body_is_declined_and_not_counted` (new — a declaration whose refs are not there is declined under its own name rather than priced as one that was, which is why the three no-op classes are still decoded instead of answered on the opcode) |
| W9 | `compute_decoder.rs` — all of `runtime/decode/compute.rs`: the flat `Command` with forty fields, `Kind`'s thirty-four variants, `OpcodeConfidence`, `opcode_supported`/`opcode_apple_rejected`, the `BufferBinding`/`RefBinding`/`SamplerBinding`/`Size3`/`Region3` entry structs, `var_len`, and its thirteen tests | `runtime/decode/compute.rs` (whole file), `runtime/decode/mod.rs` (`pub mod compute;`), `runtime/compute_exec/{mod,vulkan,metal,tests}.rs`, `runtime/compute_session/{mod,metal}.rs`, `runtime/exec/{mod,vulkan,report}.rs`, `runtime/icb/tests.rs`, `backend/{mod,compute_session,vulkan/mod,metal/device}.rs`, `runtime/decode/ledger.rs`, `tests/{golden_vectors,wire_fixtures_reach_the_decoders}.rs` | `reims_vgpu_protocol::decode::compute::ComputeRecord` for the seventeen settled records, whose variants carry **named payloads** so `apply_record` matches thirteen of them and each arm takes the one record it handles; `decode::sync` for the two barriers, `decode::resource_state` for the compressed-reinterpretation flush, and `decode::residency::lift` for the unqualified residency pair. The four dispatches reach `Backend::execute_dispatch` as a `DispatchRecord`, which removed `resolve_dims_unknown_kind` — there is no not-a-dispatch left to refuse — and stopped the indirect forms reporting a grid of three zeroes they never carried. `ComputeAccum`'s three bind setters take an entry *contract* (`BufferBindEntry`, `ObjectBindEntry`) rather than the decoder's structs, so `has_attribute_stride`/`has_lod_clamp` stopped being flags beside a zero: the record with no stride field cannot produce a `Some`. `ComputeAccum::dispatch_type` is a `DispatchType` end to end. **Carries one guest-visible change** — a `writeDescriptor` ordinal outside `MTLDispatchType` is refused at the lift instead of being substituted with `Serial`, so the pass keeps the type the guest last stated; the `compute_dispatch_type_unknown` census survives at the refusal. **The eleven rows the ledger has not settled did not move**: `runtime/decode/compute_spi.rs` is new and decodes exactly them — the fence pair, the seven control-flow records and the two indirect-command executions — and refuses every settled opcode by name. Unlike the blit rail's unsettled four, these **drive real work**, and that difference is stated in its header rather than papered over. | `runtime::decode::compute_spi::{no_settled_compute_row_is_a_record_this_decoder_owns, every_unsettled_compute_row_reaches_this_decoder, a_control_marker_and_a_control_predicate_are_told_apart_by_their_length, the_two_command_buffer_executions_name_different_second_operands, the_fence_pair_keeps_its_two_directions_apart, an_opcode_no_compute_row_names_is_unknown_rather_than_settled}` (new — replace `control_do_while_start_empty_end_condition`, `control_if_while_and_icb_lengths` and `the_control_flow_condition_is_apples_own_layout`, and add the claim that keeps `dead/`'s prohibition structural: no settled row is decodable twice); `runtime::decode::ledger::every_compute_row_reaches_exactly_one_decoder_and_the_ledger_picks_it` (new — replaces `the_compute_decoder_recognises_every_compute_operation_the_ledger_records` and `the_compute_opcode_table_is_apples_compute_manifest_plus_four_named_exceptions`, and is strictly stronger: the rail has five decoders now, so "no row is dropped by all of them" and "no row is claimed by two" are both asserted where one decoder could only assert the first); `reims_vgpu_protocol::decode::compute::{the_two_direct_dispatches_share_a_record_and_not_a_meaning, the_two_indirect_records_have_opposite_field_orders_and_one_meaning, the_stage_in_region_writes_its_size_before_its_origin, a_bind_borrows_its_entries_and_keeps_the_slot_it_starts_at, the_offset_rebind_carries_a_stride_only_when_its_record_does, a_bind_count_that_overruns_is_told_apart_from_a_short_head}` (already at the owner — they replace `set_buffers`, `pipeline_and_dispatch`, `a_bind_larger_than_the_deleted_cap_decodes_every_entry` and `every_compute_decode_failure_names_its_own_check`); `runtime::compute_exec::tests::{a_bind_with_no_stride_field_and_one_with_a_zero_stride_are_not_the_same_slot, a_sampler_bound_without_a_clamp_and_one_clamped_to_zero_are_not_the_same_slot, a_nil_entry_clears_the_slot_on_the_wire_path_too}` (new — the entry contract at its owner); `runtime::exec::tests::an_undeclared_dispatch_type_refuses_its_record_and_leaves_the_pass_type_alone` (new — replaces `an_undeclared_dispatch_type_is_named_and_counted_before_it_becomes_serial`, and adds the claim the substitution got wrong in the other direction: a refused descriptor must not reset a pass the guest set to Concurrent); the ~59 `runtime::compute_exec::tests` executor cases (kept, re-pointed at `DispatchRecord` and the wire entry types). `rejected_and_matrix` and `property_fuzz_opcodes` have **no replacement**: `REJECTED_85`/`REJECTED_88`/`REJECTED_C7` and the `ErrUnsupportedOpcode` they distinguished have no closure-ledger rows, so the question they asked no longer exists — `no_settled_compute_row_is_a_record_this_decoder_owns` asks the ledger's version of it instead. |
| W10 | *(nothing moved yet — the legacy render decoder shrank rather than left)* | `runtime/exec/mod.rs` (`handle_render_record`'s `Fence`, `Barrier`, `UseResource` and `UseHeap` arms), `runtime/exec/report.rs` (`note_residency_declaration`, `ResidencyWriteDeclared`) | `reims_vgpu_core::operation::classify` decides the record's class from the ledger row, and the three classes that own no stream state are answered from it: `reims_vgpu_protocol::decode::sync` lifts the fence pair and all three barrier shapes, and `decode::residency::lift` lifts all four residency declarations. **The disjointness is the reason this group is separable and it is structural**: none of the three touches `StreamAccum`, and the test asserts that rather than arguing it — the indirect-command executions look equally self-contained and are not, because they push onto `acc.execute_icb`, which is why they stayed behind. A render fence's *direction* now comes from the same answer that says it is a fence, so `render_fence_opcode_unknown` — a third case for a fence that was neither an update nor a wait — is unrepresentable. `note_residency_declaration` takes `Option`s: the heap forms carry no usage and the unqualified pair carries no stages, where the flat command had both fields on every record and filled them with zeros, so "the guest declared nothing" and "the selector has no such argument" stopped being one word on the counters whose whole job is telling them apart. **Carries one behaviour change**: a barrier record shorter than its wire head is now declined as short instead of counted as a no-op. Both answers execute nothing; the difference is which line a reading gets, and the wire crate's fixture-derived length is what says an eight-byte `memoryBarrierWithResources:` is not that record. | `runtime::exec::tests::each_ledger_routed_render_class_reaches_a_counter_that_names_which_one_it_is` (new — replaces `a_residency_or_barrier_record_is_counted_rather_than_dropped_in_silence`, whose fixtures were header-only records the wire layout says are short, and is stronger in three ways: all three barrier shapes rather than two, the read/write/empty residency split rather than empty alone, and the claim that makes the group a group — no stream state moved); `runtime::exec::tests::a_render_fence_orders_in_the_direction_its_own_opcode_names` (new — an update reaches the render domain and satisfies the wait that follows it, which is the only way to tell the two directions apart from outside the fence store) |
| W11a | *(nothing moved yet — the legacy render decoder shrank rather than left)* | `runtime/exec/mod.rs` (the eleven `RenderKind::{SetPipeline,SetViewport,SetScissor,SetBlendColor,SetCullMode,SetFrontFacing,SetDepthBias,SetDepthStencil,SetStencilReference,SetRasterState,SetVisibilityResultMode}` arms, and the `setLineWidth:` half of `SetFloatState`) | `reims_vgpu_protocol::decode::render::decode` lifts all fifteen rows, applied by `runtime::exec::handle_render_stream_state`. **The disjointness claim is different in kind from W8's and W10's and the code says which**: those classes touched no stream state at all, which is a test that separates nothing on this rail because every encoder record lands in `StreamAccum`. These fifteen are separable because each is the *sole writer* of the field it sets, so no other record class can disagree with the new decoder about a value — `is_render_stream_state` is where that claim lives and is exhaustive over `RenderKind`, so a row added to the contract must be classified rather than silently joining the legacy path. Routing is on the kind and not on `classify`: every encoder record is one `OperationClass::Render`, and `RenderKind::of_opcode` answering at all is the ledger's settlement. That is why `setLineWidth:` moved and `setTessellationFactorScale:` — the same wire form with no settled row — did not, leaving half of the legacy `SetFloatState` arm live and half a misroute. **The store actions did not move and are not in this group**: `SetStoreAction` mutates `color_slots`, `depth_attach` and `stencil_attach`, which the pass descriptor writes, so those three are one later group; the binds write the six bind tables and `unrepresentable`, and the draws read the whole accumulator. **Carries one named reading change** — a plural viewport or scissor record of count zero was `ErrBadLength`, a line saying a render record failed to decode without saying which; it is now a census with the previous state standing, which is what the refusal already did and could not say. | `runtime::exec::tests::each_render_state_record_writes_the_one_field_it_names_and_no_other` (new — replaces the per-arm value assertions that the legacy state arms carried, and is strictly stronger: it destructures the whole of `StreamAccum` so a field added to the struct fails to compile until it is named, and asserts that exactly one entry differs per record, which is the sole-writer claim itself rather than an argument for it — none of the old per-arm checks could see whether a record had also moved a field belonging to another class); `runtime::exec::tests::a_plural_viewport_or_scissor_of_count_zero_keeps_what_the_stream_already_bound` (new — pins the reading change: both counters fire and both bound states stand) |
| W11b | *(nothing moved yet — the legacy render decoder shrank rather than left)* | `runtime/exec/mod.rs` (the four `RenderKind::{SetBuffer,SetBufferOffset,SetTexture,SetSampler}` arms and `apply_binds`'s stage vocabulary), `runtime/draw/mod.rs` and `runtime/draw/metal/mod.rs` (`PastTableBind::stage`) | `reims_vgpu_protocol::decode::render::decode` lifts all twelve bind and offset rows, applied by `runtime::exec::handle_render_binds`. **The disjointness claim is weaker than W11a's and the code says which**: these twelve write the six bind tables — sole writers, as W11a's fifteen were — and also the `unrepresentable` latch, which the pass descriptor writes too. That is still a group boundary because the latch is **first-wins and its insert order is the stream's own record order**, which no change of decoder can alter, where two writers of a *value* could disagree. **`Stage::Unknown` is gone from the live vocabulary**: `RenderKind::bind_stage` is total over the bind rows, so the state the flat command reached as "the record named no stage" — and which `apply_binds` answered by counting a clear against a table it could not name, and `PastTableBind::stage_name` by answering `"unknown"` — is not one a lifted bind can be in. `apply_binds` takes its entries by reference and `make` takes `&T`, so each of the five wire entry shapes reaches its own arm: a record with no stride field cannot produce `Some(0)` and one with no LOD clamp cannot produce a clamp of zero, which is what `has_attribute_stride`/`has_lod_clamp` beside a zero used to allow. The LOD clamp travels *inside* its entry, so a slot can no longer be paired with another slot's clamp — the flat command carried two parallel lists and zipped them. | `runtime::exec::tests::each_render_bind_row_writes_the_one_argument_table_its_opcode_names` (new — twelve rows against six tables, asserting the one its opcode names moved and the other five did not, and that no row refused the stream's draws; no legacy test could make this claim, because the stage came from a field the decoder had already filled in); `runtime::exec::tests::{a_strided_vertex_bind_keeps_its_stride_and_a_plain_one_reports_none, a_sampler_bind_carries_its_own_lod_clamps_per_slot}` and the ~30 other `runtime::exec::tests` bind cases (kept, re-pointed at the lift in production — they drive `handle_render_record`, so the `None`-is-not-`Some(0)` claims they already pinned now pin them at the new owner); `reims_vgpu_protocol::decode::render::{a_bind_borrows_its_entries_and_keeps_the_slot_it_starts_at, the_offset_rebind_carries_a_stride_only_when_its_record_does}` (already at the owner) |
| W11c | *(nothing moved yet — the legacy render decoder shrank rather than left)* | `runtime/exec/mod.rs` (the `RenderKind::{RenderPass,SetStoreAction}` arms), `runtime/exec/report.rs` (`note_pass_extent_for_slot`'s `&Command` parameter) | `reims_vgpu_protocol::decode::render::decode` lifts the pass descriptor and the three store-action rows, applied by `runtime::exec::handle_render_pass_state`. **The three overrides are *in* this group and not beside it**: they mutate `color_slots`, `depth_attach` and `stencil_attach`, which the descriptor writes, so splitting them would put two decoders on one attachment's `store_action` — the disagreement the plan forbids. The descriptor is one wire view: `decode_{depth,stencil,color}_attachment` each re-derived their section's base from `size_of` sums this module also wrote down, and the eight colour slots are now an iteration over `RenderPassBody::color` rather than eight offset computations. `StoreActionTarget` is total over the three rows, so the legacy arm's fourth case — a store action for none of the three, reported as `render_store_action_opcode_unknown` — is unrepresentable. The `color0` convenience block is gone with the second reading it needed: it re-pushed slot 0 onto `clears` from a separately lifted field, guarded by "it is not already there", and the loop pushes exactly that attachment under exactly that condition, so the guard could never pass. **Carries one behaviour change** — a pass record shorter than `RenderPassBody` is refused instead of read at the offsets that fit. The legacy decoder accepted anything from `PASS_MIN_PAYLOAD` (depth, stencil, one colour slot) and answered the other seven slots as unattached, which is indistinguishable from a guest that attached one; Apple's serializer writes all 592 bytes, and the short form was a tolerance for this repo's own fixtures. | `runtime::exec::tests::a_pass_descriptor_short_of_its_record_is_refused_rather_than_read_at_the_offsets_that_fit` (new — pins the behaviour change in both directions: one byte short moves no pass state at all, and the full-length record with the same first attachment is still honoured, which is what says the refusal is about the length and not the bytes); `runtime::exec::tests::each_store_action_override_reaches_the_attachment_its_target_names` (new — one pass declaring colour slots 0 and 3, a depth and a stencil attachment, all four `DontCare`, then all three overrides; the claim the two kept tests cannot make is that slot 0 stays `DontCare` while slot 3 moves, so a cross-target landing is visible rather than merely absent); `runtime::exec::tests::{a_store_action_override_reaches_the_slot_it_names, a_depth_or_stencil_store_action_override_reaches_its_own_attachment, an_unsupported_depth_attachment_is_named_not_just_dropped, a_colour_attachment_naming_a_subresource_this_device_cannot_bind_refuses_the_draws, the_pass_extent_census_scores_either_resolve_arm}` and the other `runtime::exec::tests` pass cases (kept, re-pointed — the last three had their fixtures lengthened to the record's real 592 bytes, which is itself the change being recorded) |
| W11d | *(nothing moved yet — the legacy render decoder shrank rather than left)* | `runtime/exec/mod.rs` (the `RenderKind::{Draw,DrawIndirect}` arms and `execute_indirect_draw`'s `&Command`), `runtime/exec/report.rs` (`is_indexed_draw_opcode`, deleted; `note_indirect_draw_refused`'s `&Command`), `runtime/decode/render/mod.rs` (`wire_instance_count`, now unreached) | `reims_vgpu_protocol::decode::render::decode` lifts all fourteen draw rows, applied by `runtime::exec::handle_render_draw`. **This group moves last because it is the only *reader***: it takes `bind_snapshot`, which is everything W11a, W11b and W11c wrote, and a reader can only be trusted once every writer it reads is settled. Which indirect form a record is comes from its own variant — an `IndexedIndirect` carries an `IndexRef` and a `PrimitivesIndirect` does not — so the opcode is not read a second time, and the `1 => 4, _ => 2` index-stride restatement is `IndexType::bytes` (with `IndexType::ordinal`, new, as the inverse of `parse` for the consumers that still carry `MTLIndexType` as a raw `u32`). `is_indexed_draw_opcode` is **deleted rather than moved**: it existed only because the decoder collapsed every draw form to one `Kind`, so the opcode was the only thing that could say which class a record was — and it listed **three** of the six indexed forms, which `DrawRecord::Indexed` is now total over. **Carries one behaviour change** — `wire_instance_count`'s `.max(1)` is gone, so a selector carrying no `instanceCount:` argument draws one instance (Metal's own default for it) and a guest that wrote `instanceCount:0` gets the zero it wrote. Those were one number in the decoder, and three arms of this device disagreed about the zero: `backend::metal::render` refuses it by name — a refusal the clamp made unreachable — and `runtime::icb` already handed the same argument to Metal unclamped. The clamp read zero on every driven boot, so this is inert on measured workloads and stops the disagreement on unmeasured ones; the `draw_instance_count_zero` census survives at the lift. A count above `u32::MAX` is refused as `render_draw_count_out_of_range` rather than truncated, which is `narrow_count`'s rule at its new site. | `runtime::exec::tests::a_draw_with_no_instance_count_and_one_asking_for_zero_are_not_the_same_draw` (new — pins the behaviour change end to end: the plain form records one instance, the written zero records zero *and* raises the census, which no test could assert while the clamp made the two one number); `runtime::exec::tests::a_compact_draw_and_its_wide_encoding_record_the_same_draw` (new — the compact and wide encodings of one shape have different field orders and different widths, and must produce the same `PendingDraw`, or a guest whose count crossed 65535 would draw different geometry than the same guest below it); `runtime::exec::tests::{wide_indexed_draw_reaches_pending_draw, a_base_vertex_and_base_instance_reach_the_pending_draw, every_decoded_draw_in_a_stream_reaches_the_draw_list, an_indexed_draw_with_no_index_buffer_is_named, a_bind_after_a_draw_does_not_rewrite_that_draws_snapshot, a_dropped_draw_names_which_check_refused_not_just_its_class}` and the other `runtime::exec::tests` draw cases (kept, re-pointed at the lift in production); `reims_vgpu_protocol::decode::render`'s draw-layout tests (already at the owner — they are what makes the per-form offsets one declaration rather than six) |
| W11e | *(nothing moved yet — and this row is where the reason stops being "the decoder shrank")* | `runtime/exec/mod.rs` (`render::decode` and the eleven remaining `RenderKind` arms, including the `OtherAccepted` catch-all), `runtime/exec/vulkan.rs` (`render_pipeline_refs`), `runtime/exec/report.rs` (`note_unnamed_icb_execute`'s `&Command`) | `runtime/decode/render_spi.rs` is new and decodes exactly the thirty-one rows the ledger has not settled: the five patch draws, the two `executeCommandsInBuffer:` forms, the six pass-property records, the three store-action-options records, the two tessellation records and the twelve amplification and tile-shader records. It refuses every settled opcode by name, so no record on this rail has two readings. **After this commit no production path reaches `runtime::decode::render::decode` at all** — the Vulkan preflight's pipeline pre-scan was the last one, and it now uses the same lift the walk does, so a pre-scan can no longer disagree with the walk about which records set a pipeline. The module's three refusals answer three different questions where the legacy decoder had a catch-all: `ErrUnknownOpcode` keeps the deduped fail-visible line **and the wire capture**, because for an opcode no row names the bytes are the whole diagnostic; `ErrSettledElsewhere` is a routing bug; `ErrShort` is a record. **The legacy file does not move yet and the reason is not the usual one**: `runtime/decode/render/mod.rs` also holds the live render-pass attachment model — `ColorAttachment`, `DepthAttachment`, `StencilAttachment`, `ScissorRect`, the attachment offsets, the three `*_from_wire` lifts and `attachment_subresource_is_bindable` — which production still uses and which is not a decoder. Splitting the file is W11f, and it is what disconnects the decoder. | `runtime::decode::render_spi::tests::{no_row_a_render_kind_names_is_a_record_this_decoder_owns, every_row_this_decoder_owns_is_an_unsettled_render_row, an_opcode_no_render_row_names_is_unknown_rather_than_settled, the_wide_patch_draw_opcode_is_resolved_by_length_and_refused_without_one, the_store_action_options_are_not_wider_store_actions, an_amplification_count_reports_whether_any_mapping_moves_a_view, the_two_command_buffer_executions_name_different_second_operands, every_pass_property_record_reaches_an_arm_of_its_own, a_tile_dispatch_grid_is_not_the_threadgroup_memory_length, a_record_short_of_its_body_is_refused}` (new — they replace the legacy decoder's `the_wide_patch_draw_opcode_is_resolved_by_length_and_refused_without_one`, `the_store_action_options_are_not_wider_store_actions`, `vertex_amplification_decodes_at_the_widths_the_serializer_wrote`, `a_single_view_mapping_that_offsets_a_view_is_not_the_default`, `execute_commands_range_and_indirect`, `every_pass_property_record_reaches_an_arm_of_its_own` and `a_tile_record_is_decoded_rather_than_accepted_without_a_claim`, and add the two claims that keep `dead/`'s prohibition structural: no settled row is decodable twice, and no row is claimed here that the ledger does not carry); `runtime::decode::ledger::every_render_row_reaches_exactly_one_decoder_and_the_ledger_picks_it` (new — replaces `the_render_opcode_table_is_exactly_apples_render_manifest` and `the_accepted_window_ends_where_apples_render_manifest_does`, and is strictly stronger: the rail has four decoders now, so "no row is dropped by all of them" and "no row is claimed by two" are both asserted where one decoder could only assert the first. It is also the first of the three rail cross-checks to meet a `Closure::Refused` row, and it settles what a contract refusal claims: nothing) |
| W11f | `render_decoder.rs` — the decoder half of `runtime/decode/render/mod.rs`: `DecodeStatus`, `Kind`'s thirty-four variants, `Stage`, the flat `Command`, `DecodedBufferBind`, `wire_view`/`narrow_count`/`wire_instance_count`, `opcode_supported`/`opcode_above_the_encoder_window`/`unclaimed_accepted_opcode` and the eighty-arm `decode`; and `render_decoder_tests.rs` — thirty-nine of its forty-four tests, plus the three `runtime::decode::ledger` cross-checks that named it | `runtime/decode/render/{mod,tests}.rs` (both deleted), `runtime/decode/mod.rs` (`pub mod render;`), `runtime/exec/{mod,tests,report}.rs`, `runtime/draw/{mod,tests}.rs`, `runtime/draw/metal/mod.rs`, `runtime/decode/ledger.rs`, `crates/reims-vgpu-wire/src/ops/render_pass.rs`, `tests/{golden_vectors,wire_fixtures_reach_the_decoders}.rs` | Nothing — the decoder's replacements all landed in W11a–W11e. **What this row does is the disconnect**, and it needed a split first: the file held a decoder *and* the live render-pass attachment model, and only the decoder had been replaced. The model — `ColorAttachment`, `DepthAttachment`, `StencilAttachment`, `ScissorRect`, `AttachSubresource`, `LevelSupport`, the attachment offsets, the three `*_from_wire` lifts, `attachment_subresource_is_bindable` — is now `runtime::render_pass`, out from under `decode/` because it answers "what can this device bind" and not "what did the guest write". Five of the forty-four tests covered the model rather than the decoder and moved with it. **One defect fell out of the move and is fixed at its owner**: `wire_fixtures_reach_the_decoders::no_decoder_reads_a_bit_apples_serializer_never_wrote` began failing on `0x1a`, because `RenderRecord::WriteDescriptor` borrows the whole `RenderPassBody` and the derived `Debug` rendered the three `unwritten_above_*` fields — bytes the wire crate's own doc calls "the guest's stale ring". The three bodies have hand-written `Debug` impls now that omit them; `PartialEq` is untouched, because byte equality over a descriptor is a legitimate and different question. | The thirty-nine moved tests' replacements are the ones W11a–W11e's rows already name, and the register is where that is checked rather than assumed: the fifteen state rows by `each_render_state_record_writes_the_one_field_it_names_and_no_other`, the twelve binds by `each_render_bind_row_writes_the_one_argument_table_its_opcode_names`, the pass descriptor and store actions by `a_pass_descriptor_short_of_its_record_is_refused_rather_than_read_at_the_offsets_that_fit` and `each_store_action_override_reaches_the_attachment_its_target_names`, the draws by `a_draw_with_no_instance_count_and_one_asking_for_zero_are_not_the_same_draw` and `a_compact_draw_and_its_wide_encoding_record_the_same_draw`, the thirty-one unsettled rows by `runtime::decode::render_spi::tests`' ten, and the three ledger cross-checks by `every_render_row_reaches_exactly_one_decoder_and_the_ledger_picks_it`. Kept and re-homed: `runtime::render_pass::tests::{depth_and_stencil_pass_slots, depth_and_stencil_records_end_where_the_next_section_begins, a_scissor_covers_its_target_only_when_every_term_says_so, every_field_of_the_attachment_prefix_decides_bindability, a_colour_attachments_level_does_not_swallow_its_slice}` — the five that were about the model all along. Re-pointed: `tests::wire_fixtures_reach_the_decoders::render_verdict` (now the union of the rail's four decoders, and it lost the `Kind::OtherAccepted` arm — an `Ok` meaning "no arm claimed this", which is what once hid the LOD-bearing sampler binds behind a passing run) and `golden_vectors::corpus_property_random_decode_no_panic` |

## Seam 6's structural zeros, as a scoreboard

The plan states these as required steady-state values. It does not say which are
met, and a table of aspirations reads exactly like a table of results — so this
is where the current answer lives, with the number it was read from. Every row
below is off the driven macos-15 x86/Vulkan boot behind the last live-validation
row (172 862 draws), or off a named off-VM gate.

| Metric | Required | Now | Read from |
|---|---:|---|---|
| Threads created per EXEC, command buffer, draw, save, or watchdog | 0 | **met** | fn `a_watched_dispatch_that_returns_creates_no_thread_of_its_own`, fn `sixty_four_saves_create_no_thread_beyond_the_one_owner`; both count `/proc/self/task` after the single owner is up. The two violations were a watchdog thread per compute dispatch (`5c1be382`) and a persistence thread per pipeline-cache save (`b0bb5b8a`). |
| Heap allocations per steady-state draw | 0 | **met, for the draw** | fn `a_warm_repeat_draw_that_binds_descriptors_and_vertex_buffers_does_not_allocate`: 0 trips, 0 bytes, on a draw binding one storage buffer and three vertex attributes. Compute dispatch is not measured this way and is not claimed. |
| Full binding-table rebuilds on an unchanged draw | 0 | **met** | `descriptor_push_held=9 680` draws sent nothing, and since `191b06c9` they also *build* nothing: the write structures are derived only by the consumer that sends them. |
| Standalone stamp submissions | 0 | **met** | `gpu_stamps=24 852` against `queue_async_submits=2 404`, which equals `batch_flushes` exactly. Stamps ride a batch that was being submitted anyway; none adds a submission. |
| Native submissions per draw | 0 | **met in the sense the plan means** | `queue_async_submits=2 404` for `draws=172 862` — one submission per 72 draws, and every one of them is a batch flush, never a draw. |
| Cache misses for a live unchanged contract object after first resolution | 0 | **met to within first use** | `layout_hits=48 880`/`layout_misses=59`, `pipeline 48 610`/`329`, `pass 61 828`/`20`, `shader 97 637`/`142`. The misses are first sights: `attr_sets` settles at **14** for the boot, which is the plateau these unbounded caches are justified by. |
| Shared/global mutex acquisitions per draw | 0 | **not met** | `static ENGINE: Lazy<Mutex<EngineState>>`. Every draw takes it. This is the process-global engine named above, and nothing else on this list is blocked on anything else. |
| Global atomic census updates per draw | 0 | **not met** | `EngineCounters` is a bag of process-global atomics and a draw bumps a dozen. Uncontended today *because* the engine mutex already serialises draws, so it is the same blocker wearing a different hat: sharding the counters before the lock goes buys nothing measurable. |
| Host GPU waits for ordinary commands | 0 | **met** | `ring_retire_blocks=0` and `fence_timeouts=0` over 172 862 draws, with `readbacks=0` — no draw ever waited for a ring slot's fence, which is the only wait a steady-state draw could take. The two warm-draw gates assert both deltas at zero, so it is a standing check and not one boot's reading. `render_post_wait_skips=49 550` is the positive half: the skip path ran. |
| Wakeups whose scheduling unit is a draw | 0 | **met at the queue owner** | `queue_async_submits=2 404` for 172 862 draws, equal to `batch_flushes` — the queue owner wakes once per batch, never per draw. Only that thread is measured; a wakeup elsewhere would not appear here. |
| Re-scans of already resolved EXEC bytes | 0 | **not measured here** | |
| Repeated transfers for one range, version and direction | 0 | **met for buffers, unmeasured for images** | `buffer_bind_reuses=304 017` and `buffer_index_bind_reuses=892`: every one is a bind served from a copy the command buffer already held rather than a second transfer of the same bytes. The image side is not the same claim — `sampled_reuploads=45` at 15.3 MB are lawful, since the content version moved, but nothing here separates a lawful reupload from a repeated one. |
| Pipeline compiles on a Ready pipeline draw | 0 | **met** | fn `warm_identical_dispatch_zero_creates_and_allocs` and the `creates`/`allocs` deltas the batch suite asserts at zero on a warm draw. |
| Supported operations reaching an unknown access-mode or stage fallback | 0 | **not measured here** | |

**Do not read a "not measured here" as a zero.** Both may well hold; the
point of the column is that nobody has asked, and the plan's own argument for
this table is that an unmeasured zero and a violated one are indistinguishable
from the outside.

## Live validation

A cutover is verified against a guest, not only against a suite, and this is
where that is recorded — a row whose gates are green and whose rail has never
run under an unmodified guest has not been checked where it matters.

**How to read the absolute counts.** Route counters are per-second windows and
a boot's total is their sum. Rows landed before `37f6f953` were summed by
selecting census lines on one of the keys being summed, which silently skips
every window in which *that* key did not fire — measured at a 3.3x undercount
on `pipe_memo_hit` in one boot. Select census lines on `store_routes`, the dump
line's own marker, instead. Two keys summed over the same window set still compare
exactly, so every **equality** below stands and every magnitude is a lower
bound. A **zero** does not follow from that sum at all: a counter is only
printed in a window where it fired, so one that fires only in skipped windows
reads zero when it is not. Establish a zero by asking whether the key appears
in the log *at all* — `grep -c 'key=' /tmp/reims-vgpu-fail.log` — which needs no
window arithmetic and is exact. Every zero this file claims has been
re-established that way.

| Rows | Pathway | Result |
|---|---|---|
| W1–W7 | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-13` (x86 macOS guest, Linux/KVM host, Vulkan rail) | Guest reached the desktop; `blit_kind_b2t_us` / `blit_kind_t2b_us` / `blitrec_copy_n` non-zero, so real guest transfers ran through the lifted records; zero lines on `blit_fail`, `blit_decode`, `blit_record_wrong_class`, `blit_transfer_not_a_transfer` and `*_misrouted` across 2306 log lines. |
| W8–W9 | same | Guest reached first frame through the resident zero-copy path; the compute rail executed real work (`compute_n` up to 16 dispatches in a packet, `compute_pipeline_hits`, `compute_buffer_extent_narrowed`, guest-page writeback) and the blit and draw rails were unaffected (`blit_kind_b2t_us`, `draws`); **zero** lines on `compute_record_misrouted`, `compute_barrier_not_a_barrier`, `compute_residency_without_usage`, `compute_record_unframed`, `compute_decode_settled_elsewhere` and `compute_decode_unknown_opcode` across 4203 log lines. |
| W10 | same | Guest reached the desktop through the resident zero-copy path and the render rail did real work — `draws` up to 893 in one packet, `render_noop_barrier` up to 76, `fence` up to 14 with real wait times, and the bind reach bands (`render_bind_reach_{buffer,texture,sampler}_le`) populated. **Zero** lines on `render_record_misrouted`, `render_fence_not_a_fence`, `render_barrier_not_a_barrier`, `render_record_unframed`, `render_residency_resource_without_usage` and `render_record` declines across 7823 log lines. The four residency counters read zero, which is a reading and not a gap: this guest's compositor declares no residency on the render encoder, so the class is routed and unexercised — the barriers and the fence pair are what this boot actually validated. |
| W11a | same | Guest reached the desktop; the render rail ran 768 draws through the lifted state records, and the rail's own dynamic-state census is the positive evidence rather than the absence of failures: `dynstate_{pipeline,depth_bias,line_width,blend_constants,depth_stencil}_held=185`, `dynstate_raster_held=718`, `dynstate_viewport_held=153`, `dynstate_scissor_held=120`, `pipeline_hits=764` — every one of those is a value that reached `PendingDraw` from a record `handle_render_stream_state` lifted. **Zero** lines on `render_record_misrouted`, `render_state_record_not_state`, `render_record_unframed`, `render_record` declines and `render_decode` across 8019 log lines. The two count-zero counters this step added read zero, which is the reading their comment predicted: Metal requires at least one viewport, so a guest emitting the empty array is the case worth a line and not the case worth expecting. |
| W11b | same | Guest reached the desktop; 763 draws, and all three bind classes ran through the lifted entries — `render_bind_reach_buffer_le16` 20 records, `render_bind_reach_texture_le16` 19, `render_bind_reach_sampler_le16` 19, `vertex_buffer_bind_slots=2878` reaching the encoder. **Zero** lines on `render_record_misrouted`, `render_bind_record_not_a_bind`, `render_record_unframed`, `render_record` declines and `render_decode` across 7944 log lines, and zero on `render_*_bind_slot_past_table` and `render_buffer_offset_slot_{unbound,past_table}` — the three losses `apply_binds` can report. Every record this guest issued ended at slot 16 or below, which is the same band the pre-cutover reading found, so the stage now coming from the opcode did not move any bind. |
| W11c–W11d | same | The whole render encoder now runs through the lifted records, and this is the boot that says the pixels are the same ones. Guest reached the desktop; 788 draws, `pass_hits=788` and `layout_hits` matching them one for one, `render_pass_continuations=499`, and the scissor-coverage bands populated (`draw_cover_loaded_full_scissor=209`, `draw_cover_loaded_partial_scissor=453`) — every one of those is a value a draw read out of a `PendingDraw` the new lift assembled from a descriptor the new lift read. **Zero** lines on `render_record_misrouted`, `render_pass_record_not_pass_state`, `render_draw_record_not_a_draw`, `render_record_unframed`, `render_record` declines, `render_draw_count_out_of_range`, `render_store_action_slot_undeclared` and `render_store_action_out_of_range` across 7932 log lines. The two censuses W11c and W11d added or moved — `draw_instance_count_zero` and the short-pass refusal — read zero, which is what the clamp's own measurement predicted for the first and what a serializer that writes all 592 bytes predicts for the second. |
| W11e–W11f | same | The rail with no legacy decoder left behind it, and the boot it is recorded from did not reach the desktop — the guest kernel panicked, and the panic is not this cutover's. Two boots on this tree panicked (Dock, `CR2: 0x1e8`; NotificationCenter, `CR2: 0x0`) and a **control build of `9f58d047`, the commit before W11e**, built and pinned in an isolated worktree, panicked the same way and sooner (fontworker, `rw lock count underflow`, 618 log lines against 1579 and 2391). All three carry the three markers `journal/2026-09-01-x86-guest-panic-not-the-backport.md` established as separating a panicking boot from a clean one — `row_pitch_mismatch`, `spirv_capability added` and the `guest_bpp=16 shader_bpp=4` storage specialization — which are compute/storage-path work no render decode touches. What the render rail did do before each panic is the positive evidence: 222 draws with `pass_hits=222` and `layout_hits` tracking them, `render_pass_continuations` up to 138, `render_noop_barrier` up to 47, `pipeline_hits` up to 195, and all three bind reach bands populated (`render_bind_reach_{buffer,texture,sampler}_le16`) — every one of those read out of a record `render_spi` lifted, since no other decoder is in the tree. **Zero** lines on `render_record_misrouted`, `render_decode_settled_elsewhere`, `render_decode_unknown_opcode`, `render_record_unframed`, `render_state_record_not_state`, `render_bind_record_not_a_bind`, `render_pass_record_not_pass_state`, `render_draw_record_not_a_draw` and `render_record` declines, across both boots. A desktop-reaching boot on this rail remains owed and is recorded here as owed, not as done. |
| W11e–W11f | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail) | The desktop-reaching boot the row above records as owed. A second rail was tried because macos-13 panicked three times running, and macos-15 ran the full 420 s without one — which is also what says the panic is the rail's and not the tree's. The render rail did an order of magnitude more work than any earlier row: **6947 draws** in one packet, `pass_hits=12354`, `layout_hits=6940` and `pipeline_hits=6897` tracking them, `render_pass_continuations=6384`, `render_noop_barrier=198`, `vertex_buffer_bind_slots=18509` reaching the encoder, the dynamic-state census populated across the board (`dynstate_raster_held=6598`, `dynstate_viewport_held=5393`, `dynstate_scissor_held=5227`, `dynstate_pipeline_held=5407`), the scissor-coverage bands split 5958 full / 767 partial, and all three bind reach bands populated. Every one of those is a value that reached the encoder from a record `render_spi` lifted, because no other render decoder is in the tree. **Zero** lines on `render_record_misrouted`, `render_decode_settled_elsewhere`, `render_decode_unknown_opcode`, `render_record_unframed`, `render_state_record_not_state`, `render_bind_record_not_a_bind`, `render_pass_record_not_pass_state`, `render_draw_record_not_a_draw`, `render_record` declines, `render_draw_count_out_of_range` and `render_store_action_out_of_range`, across 8712 log lines. `draw_instance_count_zero` and the short-pass refusal read zero here too, on a workload seven times the size of the one that first predicted it. |
| *(no register row — `898f1603`, the naming authority)* | same | Recorded here because it changed live resolve and delete behaviour without moving a file: `DeviceState` owns one `reims_vgpu_core::namespace::Namespace` per task, `objects::resolve_resource` is the single declaration site, and `TaskResources` is keyed by the issued `ResourceId` rather than by the guest's reference. One model and not two, because a retired name cannot be spelled. Guest reached the desktop through the resident zero-copy path. **Zero** on all three refusals the change could produce — `object_declared_over_a_live_name`, `task_resource_slot_repointed`, `storage_extent_unrecovered` — across a denominator of **137 041** `task_resource_reexamined` comparisons, twice the largest earlier reading. The declaration census summed exactly for the third time (`declared_storage_dedicated=1566` + `declared_storage_no_bytes=72` = `backing_identity_asked=1638`), so every object this guest constructed was both nameable and declarable by the authority that now names it. |
| *(no register row — `1d52c36f`, on-demand naming)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15` | Recorded here for `898f1603`'s reason: it changed what the device does at run time without moving a file. Naming a guest reference is split from constructing it, because a namespace populated only by construction cannot answer a resource-lifetime list — the boot before this one measured **49 unanswerable refs of 1339**, and the model's list join refuses a whole packet for one unresolved ref. Guest reached the desktop. `lifetime_ref_already_named=1213` + `lifetime_ref_named_on_demand=19` = `lifetime_ref_asked=1232` exactly, with `lifetime_ref_unnameable=0` and `lifetime_ref_list_would_refuse=0` — **the residue closed completely**. Naming ahead of construction displaced nothing (`object_declared_over_a_live_name=0`), and the declaration census summed exactly for the fifth boot running (`declared_storage_dedicated=1510` + `declared_storage_no_bytes=54` = `backing_identity_asked=1564`, `storage_extent_unrecovered=0`). The same boot carries `4edca0aa`, which routes all twelve lifetime commands through `reims_vgpu_core::lifecycle::operation`; that path has no production caller yet, so this boot validates the naming door and not the bridge. |
| G1 | Nothing — no file moved. `DeviceState::active_child_mask` and `DeviceState::channels_defined_by_packet` are **gone as fields**, and the two arms that wrote the first are gone with them. | `model/state.rs`, `runtime/drain/mod.rs`, `runtime/mmio.rs` | `reims_vgpu_core::session::SessionModel`, held by `DeviceState::session` and reached through `child_domain_open` / `open_child_mask` / `drainable_child_mask`. **The first group, and it moves a lifetime rather than a decision.** `active_child_mask` answered two unrelated questions with one bit — "is this a publication domain the guest defined" and "is there work waiting here" — written by a channel definition, a locked doorbell register write, and a lock-free ring. Openness is now the model's `open_channels`, with `apply_control` as its one writer; "there is work here" stays `pending.child_mask`, which the two doorbell events write and which no longer claims a definition; the union is derived. **The envelope did not move**: stamps, ordering position and completion word stay with the drain for every class, and move when the ordering core does. What this buys immediately is the model's two guards, which this device did not have — a redefinition of a live domain and a free of an undefined one are now refusals rather than silent bit arithmetic, reported through `note_channel_transition_verdict`. | `runtime::drain::tests::a_definition_opens_the_domain_in_the_model_and_a_doorbell_does_not` (new — drives all three events that used to write the field and asserts only the definition moves openness); `runtime::drain::tests::the_channel_transitions_the_model_would_refuse_are_counted_apart` (now reads the model's verdict rather than a shadow mask); `runtime::drain::tests::a_packet_on_a_doorbell_only_domain_is_told_apart_from_one_on_a_defined_domain` (re-sourced onto `child_domain_open`). Re-pointed rather than replaced: every test that opened a channel by assigning the mask now goes through `DeviceState::open_child_domains_for_test`, which is the owner's own door — a fixture that could write openness directly would be exercising the second record this group removed. |
| G2 | Nothing — no file moved. `DeviceState::namespaces: Mutex<BTreeMap<u32, Namespace>>` is **gone as a field**, replaced by `lifecycle: Mutex<reims_vgpu_core::lifecycle::Lifecycle>`; `declare_object` returns the owner's refusal instead of always succeeding, and the twelve lifetime commands each reach `Lifecycle::apply`. | `model/state.rs` (the four namespace doors, `define_task`, `delete_task`), `runtime/objects/mod.rs` (`declare_object_name`'s task census, now the owner's refusal), `runtime/drain/mod.rs` (the nine wire-borne lifetime arms) | `reims_vgpu_core::lifecycle::Lifecycle` — namespaces, per-task heaps and session content authority in one owner, reached only through `DeviceState`'s lifetime doors. `LadderRung::NoTaskSpace` is the rung below `no_list_entry` the refusal needed. | `a_declaration_into_an_undefined_task_is_told_apart_from_one_into_a_live_task` now asserts the **refusal** rather than counting the difference; `a_task_redefinition_reads_its_root_move_from_the_model` holds `define_task`'s two routes to `Redefinition::root_moved` instead of a second copy of the previous directory; `a_deleted_name_stops_resolving_and_its_teardown_is_the_models` holds the teardown the device routes on and the name's resolution to one owner; `a_lifetime_command_on_an_undefined_task_is_told_apart_from_one_on_a_defined_task` (from `d2a90d93`) now also drives `apply_lifetime`'s refusal path. |
| G3 | `pending_stamp.rs` — `PendingStamp` (the per-drain coalescing latch, `latch` / `owed` / `discharges`), `StampVerdict` and its `and`, the latch-answering branch of `note_packet_stamp_waits`, and the three tests that walked them. The three `break`s the verdict drove, `stamp_deferred_mask` and `retry_stamp_held_timelines` went with `f0d7c6cc` itself. | `runtime/drain/mod.rs`, `runtime/drain/tests.rs` | The **ordering and publication group**, wired by `f0d7c6cc` and cleared of its last dropped-work regression by `dff03408`. Both drains put every arrived packet into `reims_vgpu_core::session::SessionModel` through `runtime::drain::admit_and_park` and take work back out through `settle_model_work`; the ring head advances unconditionally, and a packet that cannot run yet holds an ordering *position* rather than a consumer pointer. There is no latch because there is no window in which a completed packet's word is unpublished — completing a position publishes it — and no verdict because nothing chooses. `Unevaluable` is the one arm that moved rather than vanished: `admit_and_park` drops a wait naming a slot outside the stamp page before admission and counts `packet_stamp_wait_unresolvable`, which is the same asymmetry stated where a packet is admitted instead of where a ring head is moved. | `a_packet_whose_stamp_wait_is_unmet_is_parked_until_the_slot_reaches_it` drives the whole cycle end to end — admitted, parked, slot advanced, released, completed — which is what the latch and the verdict existed to approximate one packet at a time; `a_stamp_wait_naming_a_slot_that_cannot_exist_runs_rather_than_parking` keeps `StampVerdict::Unevaluable`'s claim against `admit_and_park`'s filter and asserts nothing is parked on such a wait. For the latch's own two properties the owner's tests replace the drain's: `a_wrapped_stamp_still_advances_its_slot` and `a_stamp_that_does_not_advance_its_slot_publishes_nothing` hold the wrapping-signed monotonicity `PendingStamp::latch` was taking a maximum for, and `a_stamp_wait_participates_and_a_wrapped_value_still_discharges_it` with `a_completion_stamp_answers_a_stamp_wait_from_another_packet` hold what `PendingStamp::discharges` answered — including the case it could not, a wait on a slot some *other* channel writes, which its own doc measured at 2 836 avoidable holds a boot. |
| G1 | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five applications over ssh) | The first group's own boot, and the one the row above records as owed. Guest reached the desktop and presented frames; **11 095 draws** in one packet, the largest workload any row here has measured. The group's own claim is the three counters and all three are the safe answer: `channel_transition_model_agrees=5` with `channel_open_of_open_domain=0` and `channel_free_of_undefined_domain=0` — the model's two guards, which this device did not have before the group, refused nothing the guest asked for. `child_packet_domain_defined=2180` with `child_packet_domain_undefined=0`, which is what says the other behaviour delta costs nothing: a domain that was rung and never defined is no longer re-published by the stranded-FIFO rescue, and this guest puts no packets on such a domain. Five channel transitions is the population and not a sample — it does not move under the workload, because this guest establishes its FIFO set once at accelerator start and per-application contexts share those channels. Zero lines on all three routes across 20 722 log lines, and nothing on stranded FIFOs, channel lifetime or a wedge. The device-info reply census agreed for the fourth boot running (`device_info_reply_before_any_identity=1`, `after=0`). |
| *(no register row — `0806b19e`, the declaration door)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | Recorded here because it changed a production path — every object this device names goes through it — without moving a file. `DeviceState::declare_object` takes a `reims_vgpu_core::lifecycle::Storage` instead of an `Option<BackingId>`, so the two call sites lose a duplicated flattening and the door speaks the vocabulary `Lifecycle::create_resource` takes. The half that matters is the distinction the old signature could not carry: "this object owns no memory" and "this device could not describe storage that exists" were both a `None` backing, and under the new owner the first is a claim to the content authority that the second must not make. Guest reached the desktop and presented frames; **15 937 draws**, the largest workload the register has measured. `object_declared_with_undescribable_storage=0` read **directly** — the floor `storage_extent_unrecovered` had predicted by proxy on six boots — with `object_declared_into_an_undefined_task=0` of 1095, `object_declared_over_a_live_name=0`, and `declared_storage_dedicated=1089` + `declared_storage_no_bytes=25` still summing. The same boot carries `fe2e1f94`'s two `&self` doors on `Lifecycle`, which have no production caller and are not validated by it. |
| *(no register row — `d2a90d93`, the lifetime-task census)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five applications over ssh) | The resource-lifecycle group's **last identified refusal**, counted before the group moves — the same gate G1's channel definition got. `Lifecycle::apply` answers `NoSuchTask` for eleven of the twelve lifetime commands and the refusal loses the whole packet, so the question is whether this guest ever sends one. Guest reached the desktop and was driven through three rounds of five applications: **`lifetime_command_in_a_defined_task=14 644`, `lifetime_command_in_an_undefined_task=0`**, with `object_declared_into_an_undefined_task=0` of 3902 re-confirmed on the same boot and no line on the failure channel. Every lifetime command this guest sends names a task a `CmdDefineTask2` opened. The namespace refusals are *not* in this reading and the drain's module doc says why: after the move the same `Lifecycle` answers the resolution and the apply, so `Refusal::Namespace` is unreachable by construction rather than unobserved. |
| G2 | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five applications over ssh) | The group's own boot, and the first on which the semantic model — not this device — owns every object name. Guest reached the desktop and drove real work: `draw_scissor_full=42 769` + `draw_scissor_partial=38 270`, `walk_records_render=34 624`, and **no line at all on the failure channel**. Every route the cutover added read zero: `lifetime_command_refused`, `lifetime_operation_unresolved`, `lifetime_effect_unacted`, `lifecycle_transfers`, `lifecycle_storage_freed`, `object_declared_into_an_undefined_task` and `no_task_space` are all absent. The positive evidence is that the model did the work rather than that nothing broke: **`lifetime_command_in_a_defined_task=15 177`** and **`object_declared_into_a_defined_task=4085`** all through `Lifecycle::apply`; the teardowns the device routes on are the model's (`object_teardown_now=2268`, `object_teardown_no_storage=428`, `object_teardown_held_by_peer=2`); and `map_audit_consistent=8822` with no finding, which is the audit run off `Remap::established` rather than off a second reading of the opcode. One known floor, now counted rather than inferred: of 204 re-points, **170** reached the model as a `ReplacePhysical` and **34** did not — `replace_physical_storage_undescribable`, a reference whose new storage this device cannot describe, whose content authority therefore stays on the old pages. |
| *(no register row — `12da7555`, the class census)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | The **ordering and publication group's** denominator, taken before that group moves, and it found the group's one blocker rather than confirming a zero. `SessionModel::admit` refuses `UnknownCommand` and `UnestablishedContract`, both of which are `transaction::classify` answering `None`. This boot: **`packet_class_exec=20 802`, `packet_class_lifecycle=13 505`, `packet_class_present=2140`, `packet_class_query=13`, `packet_class_control=11`** — 36 471 classified — and **`packet_class_unclassified=2166`, every one of them child `0x28` `CmdDeleteObject`**, whose ledger row is `Closure::Unresolved`. Five and a half per cent of the stream, one opcode. The kind census then says the open question is **10 packets wide**: 2148 of the 2166 delete a sampler state, 4 a depth-stencil state and 4 a render pipeline state — the three kinds this device tracks and retires (`sampler_state_deleted=2143`, `ds_state_deleted=4`, `pipeline_state_deleted=4`) — and the eight untracked kinds are 5 functions, 3 compute pipeline states and 2 fences, exactly `cmd_delete_object_unimplemented=10`. The row holds back 2156 packets whose ref space is already resolved in order to be honest about 10 whose is not. The traffic is also over half exec, which puts the group's risk in the class whose payload the bridge cannot build at all — see `ingress::Gap::ExecResolution`'s third input. |
| *(no register row — `c2d7ac4d` then `ca38c412`, the fence delete)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | Recorded here because it changed live behaviour without moving a file, and because it is the census method applied to a *bug* rather than to a cutover. The question was whether the serializer's fence ref space and the ref a command stream's fence record carries are the same numbers — the same question `apply_delete_object` records answering **no** for the object table. `c2d7ac4d` counted it and did nothing: **`delete_fence_ref_live=2`, `delete_fence_ref_absent=0`**, both in the render domain. So they coincide, and `ca38c412` retires the generations. The hazard removed is a wrong answer, not a leak: a wait is satisfied when the stored generation is at or past its target, so a generation outliving its fence made the next fence to get that ref pass its first wait with nothing behind it. Validation boot: desktop reached, `draw_scissor_full=40 378`, **no line on the failure channel**, `delete_fence_ref_live=2` / `_absent=0` again, and `cmd_delete_object_unimplemented` down from **10 to 8** exactly as the kind census predicted — the two fences left it, and the 5 functions and 3 compute pipeline states did not. |
| *(no register row — `ac180b8d`, the destroy ref space)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | The third and last of this session's ref-space questions, and the one that closes the `CmdDeleteObject` investigation down to a residue. For the eight destroys this device retires nothing for — 5 functions, 3 compute pipeline states — the census reports the **pair**: the type the destroy opcode names against the type the guest's own object-list entry carries, because a resolving integer is a collision (the object table's boot found 22 such) and an agreeing type is an identity. Reading: **`delete_object_ref_no_list_entry=8`**, with `delete_object_ref_type_agrees` and `delete_object_ref_type_differs` both silent. Not one of them names anything in the object list, so there is nothing for a ref-keyed retirement to find and nothing this device is failing to do. Desktop reached, `draw_scissor_full=39 879`, no line on the failure channel, `cmd_delete_object_unimplemented=8` and `delete_fence_ref_live=2` unchanged from the previous boot. What remains unresolved on the row is now enumerable: the five kinds a driven guest has never sent. |
| *(no register row — `5b7e46fb`, `e0b3cc11`, `b4327ac7` then `85b7a63c`, the backing re-point)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | G2's own coverage hole, opened by the previous session's `replace_physical_storage_undescribable=34` and closed over four boots by the same census method. The one counter was split into a refusal enum (`RepointStorageRefusal`) with a route per reason; boot one said **all 32 were `NoBytes`**, so nothing was leaving the model's content authority stale — but the 32 coincided exactly with `_via_mapping` and `_dropped`. Boot two asked whether the same integer named a live mapping surface: **31 of 31, yes**. Boot three asked the **type pair** — what the guest's own list holds at that integer — because a resolving integer is a collision and an agreeing type is an identity: **`_no_bytes_backing=36` of 36**, every other kind silent. So a re-point the model never heard about was always a surface-backing object, and its slot *is* the mapping id by construction (`backing_claimant_tasks` finds a surface's owner by probing exactly that slot for exactly that type). `85b7a63c` routes `OBJECT_TYPE_BACKING` through `mapping_backing_id` at its own slot and takes the extent from the backing's own record. Validation boot: **every `replace_physical_storage_*` refusal route silent**, `replace_physical_teardowns=189` = `unknown_object=158` + `via_mapping=31` — the 31 that were unheard now reach the model — desktop reached, host-driven workload completed, **0 FAIL lines**. |
| *(no register row — `e7708079`, `0254c64a` then `de73a9c2`, the destroy's name)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | The ordering group's last blocker, measured to its cause and fixed. `CmdDeleteObject`'s ledger row is unresolved, so `classify` gives it no class and 5.5% of the stream would refuse at admission — affordable for the channel now that a refused packet's completion word is the caller's (`d818a425`), but not for the work, since this device retires a sampler, depth-stencil, render-pipeline or fence registry entry on every one of them. Boot one asked the type pair of the kinds it *does* retire: **2123 of 2203 named no object-list entry**, which reads like two ref spaces and is not — `objects::resolve_sampler_state` looks that same ref up in the guest's list and requires a `SERIALIZER_OBJECT` there, so the spaces are one and the reading is about *time*: the guest clears its slot before it sends the destroy. Boot two asked the question that actually decides it — can the **model** name the reference, which `name_resource` answers from `object_name` before consulting the list: **zero of 1984**, because a sampler is built out of the list once and answered from `task_sampler_states` forever, and no path on it ever declared a name. `de73a9c2` takes the name at construction, through the same door the production `RefResolver` answers with. Validation boot: **`delete_object_retained_ref_model_named=2006`, `_unnamed=15`**, against `sampler_state_deleted=2006` — every sampler this device retires is now one the model can name. Desktop reached, `draw_scissor_full=39 279`, `slot_recheck` reporting no recycled-slot disagreement, **0 FAIL lines**. The residual 15 are the depth-stencil, render-pipeline and fence kinds, whose construction paths are rail-local or hold no list entry at all. |
| *(no register row — `b00224e8` then `9d81936e`, the destroy's class)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | The ordering group's blocker, closed. `CmdDeleteObject` was the one unresolved row a driven guest sends, and `classify` giving it no class kept 5.5% of the stream out of the semantic model. `b00224e8` gave the protocol crate the record's meaning — which of eleven kinds, and whether that kind's serializer row is settled — and `9d81936e` made the packet an operation: `LifecycleKind::DeleteObject` resolving through `object_destroy`, `LifecycleOp::DeleteObject` carrying the kind because the executor's half of the work differs by it, and `ResolveRefusal::UnsettledDestroy` refusing **one packet** for the five kinds no boot has ever sent instead of the whole class. Validation boot: **`packet_class_unclassified = 0`** — exec 20 758, lifecycle 15 483 (up from 13 505, which is the destroys arriving), present 2117, query 13, control 12 — against `sampler_state_deleted = 2120` and `delete_object_retained_ref_model_named = 2120`. Desktop reached, `draw_scissor_full = 39 945`, **0 FAIL lines**. What is left under `ingress::Gap::Unresolved` is four rows a driven guest does not send: `CmdDebug`, two display commands and `CmdDelay`. |
| *(no register row — `02beb035`, `cb417cb7` then `4c557186`, the ordering plane's next two blockers)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | Two things `SessionModel::admit` would have refused a real guest for, found by reading the refusal list against what production actually holds rather than by running it. **The root domain**: `open_channels` started empty and only `CmdDefineFifo` added to it, and `CmdDefineFifo` arrives on the root FIFO — so the cutover would have refused `ChannelNotOpen` on the command that opens every other domain. `ChannelId::ROOT` is now opened by `SessionModel::new` and cannot be freed. **The pipeline table**: `admit` refuses an exec whose stream binds a pipeline the table does not hold, and nothing had ever declared into it — every exec packet a real guest sends, over half the stream. `4c557186` declares at `runtime::draw::load_render_pipeline`, the one door both rails reach before either starts host work, and retires on `CmdDeleteObject`'s render-pipeline kind. Validation boot: **`pipeline_declared=134`, `pipeline_declared_already=2657`, `pipeline_declared_unnamed=0`** — every pipeline a driven guest binds has a name the model can hold — and `pipeline_retired=4` against `pipeline_state_deleted=4`. Desktop reached, `draw_scissor_full=39 223`, `packet_class_exec=21 182`, `packet_class_unclassified=0`, **0 FAIL lines**. What is still missing is the rails advancing a declared pipeline to `Ready`: until they do, an admitted exec would park on a compilation nothing finishes. |
| *(no register row — `806faadd`, the pipeline build)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five apps) | The other half of the previous row: the table filled, so `admit` stopped answering `Absent` to every exec and would have started answering `Pending` to every exec forever. Both rails now walk a declared pipeline `Declared → Translating → Compiling → Ready` through one rail-neutral pair, `draw::advance_pipeline` and `draw::refuse_pipeline`, and refuse with a reason where they cannot build it. Validation boot: **`pipeline_declared=135` = `pipeline_translating=135` = `pipeline_compiling=135` = `pipeline_ready=135`** — every pipeline this guest declares becomes usable, none is stranded mid-build — with `pipeline_refused=0` and `pipeline_advance_unnamed=0`. The declines account exactly: `pipe_memo_miss=142` against 135 first declarations leaves 7 re-resolutions, each of which declines all three steps, and `pipeline_advance_declined=21` is 3 × 7. `pipeline_retired=4`, `pipeline_retire_released=0` (nothing is admitted into this model yet, so nothing was parked). Desktop reached, `pipe_memo_hit=32 400`, **0 FAIL lines**. |
| *(no register row — `4fc4e814` then `14a1371a`, the completion word)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five apps) | The scheduler's other wait kind. `admit` parks a packet whose header names a point another packet must publish, and nothing in production called `Scheduler::publish` — so the cutover would have parked every packet the guest orders behind a fence, exactly as the empty pipeline table would have parked every exec. `4fc4e814` published at `write_stamp`, and its boot read healthy about the wrong set: **13 702 publications over four slots, 0 behind, 0 repeat** — while `stamp_write_forward` ran **14 245**, and a device publishing fewer words than it writes is missing a writer. It was the root FIFO, which writes slot 0 inline rather than through `write_stamp`; about 160 words a boot, on the timeline the measured root stamp waits are on. `14a1371a` put both records — the device's owed-word ledger and the ordering plane's timeline — behind one function called at both sites, so the next writer cannot record one and not the other. Validation boot: **`stamp_publish_first=5`** (four child timelines and the root, where it was four), `stamp_publish_advanced=12 753`, **`stamp_publish_behind=0`** — this device never writes a word behind what the model holds — against `stamp_write_forward=12 748` and `root_stamp_ordered_gpu=159`. Desktop reached, `pipeline_declared=127` = `pipeline_ready=127`, **0 FAIL lines**. |
| *(no register row — `9ab40542` then `fb76145e`, the publication point)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five apps) | The census asking whether the ordering plane would answer a stamp wait the way this drain does, and the defect it found on its first boot — which was in the commit two rows above. **`stamp_wait_model_ahead = 12 824` against `stamp_unmet_queued = 12 824`**, the same number twice: `write_stamp` hands the word to the GPU-ordered rail and returns, `Queued` is the device ledger's own name for exactly that state, and the plane's timeline was being published at that line. Twelve thousand times a boot the plane called a wait satisfied while the guest could still read the old value — a cutover publishing there releases work ordered behind GPU work that has not finished. Submission is not completion. `fb76145e` splits the records along the line they were always about: the ledger stays at `write_stamp`, and `note_stamp_visible` tells the plane at the three places a word is observed to have *landed* — the two inline page writes and the drain's own read of the slot. Validation boot: **`stamp_wait_model_ahead = 0`** and **`stamp_wait_model_agrees_unmet = 17 674 = packet_stamp_wait_unmet`** — the plane now releases nothing this device holds. `stamp_visible_observed = 30 584` against `inline = 178` and `root = 12`, `stamp_publish_behind = 0`, `pipeline_declared = 134` = `pipeline_ready = 134`, **0 FAIL lines**. `stamp_wait_model_behind = 5207` of 12 910 met waits is the *expected* remainder and not a hang: it is the first wait after a queued word lands, before the read teaches the plane — and because the publication runs in the same call as the read, a cutover that decides after publishing pays no round trip for it. **That ordering is the constraint the switch inherits from this row.** |
| *(no register row — `7edac094` then `39da38fa`, the destroys the model could not name)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five apps) | Nineteen destroys a boot, of about 2160, arriving with no name the model can hold — every one a packet `SessionModel::admit` refuses with `UnknownRef`. `7edac094` added the per-kind census first, because two counters that say *how many* cannot aim a fix, and the composition it measured was **not** the one an older reading recorded: depth stencil 4, sampler state 5, **function 5**, **compute pipeline 3**, fence 2. The old list named render pipeline (now zero — its declaration door names it) and missed function and compute pipeline entirely, so two rail edits would have gone to four packets. `39da38fa` names the three closeable kinds at their own construction sites — `mtlb::load_mtlb`, `compute_exec::load_compute_pipeline`, and one `draw::name_depth_stencil` shared by the two rail loaders, because a route pair written twice is how the second rail spells it differently. Validation boot: **`delete_unnamed_depth_stencil_state`, `delete_unnamed_function` and `delete_unnamed_compute_pipeline_state` all 0**, from 4, 5 and 3; the total falls **19 → 7**, and the seven are `delete_unnamed_sampler_state = 5` and `delete_unnamed_fence = 2` — the two kinds with no moment where the list entry and the model's namespace are both available. Naming took `function_model_named = 6051`, `compute_pipeline_model_named = 393`, `ds_state_model_named = 33`, each with **zero** unnamed. `draw_scissor_full = 40 308` against 39 621 the boot before, so the added lookups cost no frames. `pipeline_ready = 131`, **0 FAIL lines**. |
| *(no register row — `407112da`, `36186308` then this commit, the stamp wait's owner)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five apps) | The first wiring step on the stamp side, in the plan's sense: the drain keeps its ownership — the retry, the hold bit, the head pointer — and loses only its duplicate arithmetic. Whether a published point discharges a wait is one wrapping rule and it was written twice, in `drain::StampWait::satisfied_by` and in `reims_vgpu_core::identity::StampValue::reached`, character for character; two implementations of a wrapping comparison are two silent chances to disagree about which of two numbers is later, and the two failures are opposite — one parks a timeline forever on a value already written, the other releases work early. `407112da` took the *verdict* from `DeviceState::published_completion_stamp` and kept the drain's own answer beside it as a witness; `36186308` made the device's remaining callers — `PendingStamp::discharges` and `StampLedger::classify` — defer to the owning type rather than restate it. Validation boot: **`stamp_wait_owner_differs = 0` against `stamp_wait_owner_agrees = 29 852`** — the two answers never parted — with `packet_stamp_wait_met = 12 540` + `unmet = 17 312` summing to the same 29 852, `stamp_publish_behind = 0` and `stamp_wait_model_ahead = 0`. Desktop reached, `draw_scissor_full = 39 820`, `pipeline_ready = 135`, the unnamed destroys holding at sampler 5 + fence 2, **0 FAIL lines**. The witness is removed in this commit: it fires only where `stamp_publish_behind` does, which makes it a second name for one event. |
| *(second rail — `806faadd` .. `dc70dd61`, everything above)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-26` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload | A different guest driver, chosen because this rail clears its object-list slot while the packet that named it is still undrained — which is exactly where naming at construction could behave differently, and where the rows above were all established on `macos-15`. It does not: **`function_model_unnamed`, `compute_pipeline_model_unnamed` and `ds_state_model_unnamed` are all 0** over 6851, 469 and 33 namings, and the unnamed destroys are `sampler_state = 5` alone — this rail's guest never sends the two fence deletes `macos-15` does. It is also the first rail to exercise the *refusal* arm on a real guest: **`pipeline_refused = 1`, `pipeline_compilation_failed = 1`**, one pipeline this device cannot build, refused terminally with a reason instead of leaving every exec that binds it parked on a compilation nothing finishes. The table balances — `declared 210`, `translating 208` = `compiling 207` + the one refusal, `ready 207`, `retired 13` — with two pipelines resting at `Declared`, which are pipelines nothing ever drew with and so nothing ever binds. `pipe_memo_hit = 67 969` against `miss = 367`, `stamp_publish_behind = 0`, `stamp_wait_model_ahead = 0`, `packet_class_unclassified = 0`, desktop reached, **0 FAIL lines**. |
| *(no register row — `8aeb713a` then `37f6f953`, what the table is holding)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-26`, host-driven workload | The counters said what *happened* to a pipeline and nothing said what the table was *holding*, which is the difference between a healthy boot and one accumulating builds nobody is running — the same argument `backing_outstanding_census` is emitted beside `store_routes` for, and it matters more here because `Declared`/`Translating`/`Compiling` are the three states a transaction waits on. `8aeb713a` added the occupancy line; its first boot showed **170 retirements against a table holding 116**, and `37f6f953` split the two facts the ending doors had been returning one empty list for. Validation boot: **`pipeline_declared` = `translating` = `compiling` = `ready` = 260**, and `pipeline_retired = 160` matching the occupancy line's `retired = 160` exactly, against `pipeline_retire_absent = 102` — render pipelines the guest deletes that this device never drew with, so never declared. Final occupancy `pending=0 ready=100 retired=160 total=260`: **nothing parked on a build.** `stamp_visible_observed = 136 709` equals `packet_stamp_wait_met + unmet` exactly, so every wait evaluation publishes once; `stamp_wait_model_ahead = 0`, `stamp_publish_behind = 0`, `packet_class_unclassified = 0`, `pipe_memo_hit = 227 427` against `miss = 427`, **0 FAIL lines**. This is also the boot that found the summing error the preamble above now warns about. |
| *(no register row — re-verification of `806faadd` .. `134f5263` on the rail every row above was established on)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload | The rows above were measured with the summing error the preamble names, and their **zeros** were the claims that error could actually falsify — a counter is printed only in a window where it fired, so one firing only in skipped windows reads zero when it is not. Every zero re-established here by presence rather than by sum: `stamp_wait_model_ahead`, `stamp_publish_behind`, `pipeline_refuse_already_ended`, `delete_unnamed_{depth_stencil_state,function,compute_pipeline_state,buffer,texture}`, `{function,compute_pipeline,ds_state}_model_unnamed`, `pipeline_{declared,advance,refuse,retire}_unnamed`, `pipeline_refuse_not_a_build_decline`, `delete_unnamed_not_a_destroy` and `packet_class_unclassified` **do not appear in the log at all**, while `stamp_publish_repeat` appears 152 times — which is what says the method can tell the two apart. Magnitudes, summed correctly this time: `pipeline_declared` = `translating` = `compiling` = `ready` = **131**, occupancy `ready 127 + retired 4 = total 131` with `pending = 0`, `pipeline_retired = 4` against `retire_absent = 0` (this rail's guest deletes only pipelines it drew with, unlike `macos-26`'s 102), `stamp_visible_observed = 30 125` = `met 12 435 + unmet 17 690` exactly, `stamp_publish_first = 5` for four child timelines and the root, `function_model_named = 5876`, `compute_pipeline_model_named = 422`, `ds_state_model_named = 34`, the unnamed destroys holding at sampler 5 + fence 2, `pipe_memo_hit = 101 799` against `miss = 140`, `draw_scissor_full = 39 558`, **0 FAIL lines**. The one zero that cannot be re-established is `stamp_wait_owner_differs`, whose counter `9fbe427b` removed — but `36186308` had already made both sides of that comparison call one implementation, so it could differ only on a rewind, and the rewind population is `stamp_publish_behind`, which is zero by presence on both rails. |
| *(no register row — `be21e4ab` then `b1259eb4`, the destroy's reference)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five apps) | The last of the ten `session::Refusal` variants that a real guest could still have hit, and the boot that says it cannot. `object_destroy` refused a `DeleteObject` whose reference the namespace could not resolve, and two of the eleven kinds have no construction moment where a name could be taken: a **fence exists only when an exec stream first signals it** — `set_fence_generation` is the sole writer, there is no creation command — so no namespace can ever name one, and refusing its destroy is not a safe failure but a *wrong answer*, since the stored generation outlives the fence and the next fence to reuse that reference starts life already signalled. `be21e4ab` first split the two questions the census had been asking as one, because they are not the same packets: whether *the model* has a name yet (`object_name`) is not whether *the production bridge* would refuse (`name_resource`, which falls through to the guest's own object list). `b1259eb4` then made `LifecycleOp::DeleteObject` carry `object_ref: u32` beside `resource: Option<ResourceId>` — the effect is keyed by the reference, and `resources()` answers an empty slice when unnamed, which is safe because a resource this model cannot name is one nothing else in it names. Validation boot: **`delete_unnamed_but_list_still_has_it` does not appear in the log at all** — no destroy is refused for a reference the guest's list still holds — against `delete_unnamed_and_unresolvable = 7`, which is `delete_unnamed_sampler_state = 5` + `delete_unnamed_fence = 2`, the two structurally unnameable kinds, and matches `delete_object_retained_ref_model_unnamed = 7` of `_named = 2050`. The five kinds no driven boot has ever sent are still zero by presence (`delete_unnamed_{buffer,texture,heap,rasterization_rate_map,indirect_command_buffer}`), so `UnsettledDestroy` remains a one-packet refusal for work no guest here does. This boot is a regression check and not evidence for the change — nothing in production calls `object_destroy` yet — and it regresses nothing: `pipeline_declared` = `translating` = `compiling` = `ready` = **133** with `retired = 4` and `retire_absent = 0`, `stamp_visible_observed = 29 500` = `met 12 660` + `unmet 16 840` exactly, `packet_class_unclassified`, `stamp_publish_behind`, `stamp_wait_model_ahead` and the four `pipeline_*_unnamed` all absent from the log while `stamp_publish_repeat` appears 141 times as the control, `pipe_memo_hit = 93 877` against `miss = 141`, `draw_scissor_full = 38 201`, **0 FAIL lines** across 21 886. It also reads the plan's second open contract question, which was instrumented and unread: **`query_reply_scanned = 12` = `query_reply_outside_every_allocation = 12`, and `query_reply_inside_an_allocation` is absent entirely** — on this rail no query's reply destination lands inside an allocation this device already identifies, so a reply given a backing of its own orders against nothing else. `backing_id_heap_placed` is still zero here, so a heap's extent stays unrecovered and refused by name; rails 11, 12, 14 and 26 are untried for it. |
| *(no register row — `dc99ef8c`, the per-record sink)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five apps) | A behaviour-preserving split of `walk_stream` into a framing loop and a `StreamSink`, so the walker over an exec stream can be replaced rather than duplicated — and the boot that says the split did not move a record. Desktop reached, workload completed, **0 FAIL lines across 23 223**. `walk_records_render = 530 623`, `walk_records_compute = 1085`, `walk_records_blit = 750` all still reach their handlers through the sink, `seg_chain_none = 19 921` with the other three chain routes at zero (this guest continues no encoder across a segment), `draw_scissor_full = 39 995`, `pipeline_declared = ready = 125`, `retired = 4`, `stamp_visible_observed = 30 242` = `met 12 439` + `unmet 17 803` exactly, `pipe_memo_hit = 89 818` / `miss = 134`, `packet_class_exec = 20 873`. The two losses the sink can newly name are **absent from the log entirely** — `exec_compute_record_unopened` and `exec_segment_unended` — which is what says the framing loop pairs its `begin_segment`/`end_segment` calls correctly on a real guest, and `compute_session_finish`, `stream_frame_fail` and `stream_record_fail` are absent too. |
| *(no register row — `dc99ef8c` + `5a6dea05` on two further guests)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-26` and `--rail macos-14` (x86 macOS guests, Linux/KVM host, Vulkan rail), host-driven workload | The sink and the read/execute split on guests neither was written against. **macos-26** (Darwin 25.6.0): desktop reached, **0 FAIL lines across 56 903**, `walk_records_render = 1 060 053` / `compute 1422` / `blit 754` all still reaching their handlers through `StreamSink`, `seg_chain_none = 40 190` with the other three chain routes at zero, `pipeline_declared = 138` / `ready = 137` / `retired = 26` against `retire_absent = 20`, `stamp_visible_observed = 110 751` = `met 38 958` + `unmet 71 793` exactly, `pipe_memo_hit = 174 923` / `miss = 183`, `packet_class_exec = 41 150`, `lifecycle = 57 410`, `present = 7115`, `control = 14 956`. **macos-14** (Darwin 23.6.0): desktop reached, **0 FAIL lines across 58 442**, `walk_records_render = 1 096 063`, `draw_scissor_full = 52 431`, `stamp_visible_observed = 112 464` = `met 39 522` + `unmet 72 942` exactly, `packet_class_exec = 42 542`. On both rails the two losses the sink can newly name — `exec_compute_record_unopened` and `exec_segment_unended` — are **absent from the log entirely**, as are `compute_session_finish`, `stream_frame_fail`, `stream_record_fail`, `packet_class_unclassified`, `stamp_publish_behind`, `stamp_wait_model_ahead` and `delete_unnamed_but_list_still_has_it`. The query-reply reading replicates: `query_reply_scanned = 15` = `query_reply_outside_every_allocation = 15` on each, with `query_reply_inside_an_allocation` absent — three rails now say a reply destination never lands inside an allocation this device identifies. `backing_id_heap_placed` is absent on both, so a heap's extent stays unrecovered on macos-14 and macos-26 as well. **The two guests are distinct and that was checked rather than assumed**: they report identical `packet_class_control = 14 956` and `pipeline_declared = 138`, which reads like one log summed twice, but each fail log holds exactly one `rail_selected`, the disk images differ (30.8 GB against 41.1 GB), the serial logs say Darwin 25.6.0 and Darwin 23.6.0, and `packet_class_exec` and `draw_scissor_full` do differ — the two that match are deterministic sets, the cursor-glyph upload `op0x04` carries (14 949 of that rail's 14 956 control packets, against 11 on macos-15) and the compositor's pipeline set. |
| *(no register row — the drain loops' ring read as one named step)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five apps) | Both drain loops' bound-header-snapshot-decode replaced by `Ring` + `arrival`, which is the hottest read this device performs and the one place a mistake stops every packet. Desktop reached, workload completed, **0 FAIL lines across 23 577**. Every ring fault this step can report is **absent from the log entirely** — `packet_bad_size = 0`, and no `MalformedRootPacket` or `MalformedChildPacket` of any fault, so no read regressed into one. The traffic is the same shape the earlier macos-15 rows measured: `walk_records_render = 585 095` / `compute 1097` / `blit 763`, `packet_class_exec = 22 147` / `lifecycle = 16 778` / `present = 2393` / `control = 11`, `draw_scissor_full = 40 083`, `pipeline_declared = ready = 131` with `retired = 4`, `stamp_visible_observed = 32 124` = `met 13 031` + `unmet 19 093` exactly, `pipe_memo_hit = 101 289` / `miss = 141`. `packet_stamp_wait_held = 18 885`, which is the counter the present-backpressure and stamp-hold `break`s still drive — the gate now reads `packet.opcode` instead of peeking the raw header, and it holds the same packets. The one `TRANSPORT sync_exec_lock_hold` is the first-draw pipeline compile the 2026-09-01 and 2026-09-02 journals already record at 2.9 s and 2.06 s; this boot's is 1.37 s. |

The macOS/arm64 rows of the pathway table in `AGENTS.md` cannot be run on this
host and are outstanding qualification for every row above, as the plan's
2026-09-02 amendment records. Compilation on those arms is covered by the
cross-compiled `aarch64-apple-darwin` feature-matrix arms and is not the same
claim.
| G3 | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15`, host-driven workload (three rounds of launching and quitting five applications over ssh) | The ordering group's own boot, and the first on which **this device drops no guest work at all**: `packet_unadmitted` does not appear in the log, and neither does any `FAIL` line. Every packet the guest sent went into `SessionModel` and came back out of it — `parked_taken_ready = 26 224`, `walk_records_render = 276 698`, `pipeline_declared = 143` all ready. The stamp plane carried 9366 waits (`packet_stamp_wait_met = 4000`, `unmet = 5366`) with `stamp_visible_observed = 46 255`, and the two counters that would say the plane and this device disagree — `stamp_wait_model_ahead`, `stamp_wait_model_behind` — appear nowhere in the log, nor does `packet_stamp_wait_unresolvable`. `f0d7c6cc`'s two named regressions were closed by `74f00590` (741 refused deletes), `2d4ec9e3` (the refused backing retirement) and `dff03408` (the seven refused exec windows); the first of those three shipped a deadlock that `416f9d6c` fixed after a boot measured it at `walk_records_render = 42`. The reading that made the group's own switch worth trusting is `packet_stamp_wait_met_pending`, which the moved latch's doc predicted at zero and which is absent here for the reason that doc gives — the waits this guest sends are on slots *other* channels write, and a per-drain latch could never have answered one. |
| G3 (rail sweep) | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail <r>` for **all six** provisioned x86 rails, Vulkan | The ordering group proven on more than the rail it was written against, and it found a defect on the one rail nobody had booted it on. **macos-13, macos-14, macos-15**, host-driven workload: `packet_unadmitted` and `FAIL` absent from every log, `walk_records_render` 160 519 / 153 002 / 286 140. **macos-11 and macos-12** reached a first frame through the model and are boot-only — macos-11's sshd did not answer inside the 7-minute testing window and macos-12 refused the key — with `packet_unadmitted` and `FAIL` absent from both. **macos-26** refused 918 exec packets on the first boot and 9375 on the second: `pipeline_absent_retired`, the guest deleting render pipelines and continuing to bind them, which `6587f618` took to **17** (an unresolvable exec ref, and one fragment shader this device cannot translate) while taking `walk_records_render` from 133 630 to 218 466. The sweep is the row: five rails agreeing says nothing about the sixth, and the sixth is where the group's remaining defect was. |
| *(no register row — this commit, the compute half of the pre-scan)* | `vm/boot-x86.sh --device reims-vgpu-pci --interactive --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five apps) | The correction `94838490` made to the render arm, applied to the compute arm — and the boot that first said which arm the measured loss was on. `94838490`'s own driven boot is the row's baseline (`/tmp/fail15-c0b667b8.log`, not committed): it is clean — `packet_unadmitted` 0, `FAIL` 0, `rail_selected` 1 — and it **did not** move `m2v_translation_pending_at_sync_boundary`, which stood at 2 before and after. Both of those two lines read `compute_linux_m2v … stage=kernel`. **They are compute kernels, and the render arm could not have fixed them.** Meanwhile `preflight_compute_translations` still byte-scanned each stream while admission readied the walk's *class-blind* `pipeline_leases` — which holds compute binds — on that scan's verdict, so the compute half had exactly the defect the render half had just lost, with three ways to miss a kernel: a selector carrying no threadgroup size (`DispatchThreadsIndirect`, where the flat command offered a zeroed field the scan then rejected as an out-of-range extent — the same word it uses for "the guest asked for a grid of nothing"), a dispatch after an encoder continuation (the scan reset its bound ref at every framed segment; a stream is one encoder however many segments carry it), and a framing refusal it swallowed. `reims_vgpu_core::exec::ExecWork::compute_dispatch_translations` answers all three from records the walk already resolved, and says the first structurally: `DispatchOp::ThreadsIndirect` has no such field to read. `preflight_translations` now takes `(render_pipelines, compute_dispatches)` and **reads no stream at all**. Validation boot: desktop reached, workload completed, **`packet_unadmitted` = 0, `FAIL` = 0** across 5.85 MB, `rail_selected` = 1, `pipeline_declared = 144`, `parked_taken_ready = 26 642`, `walk_records_render = 270 724` / `compute 1085` / `blit 1582`, `pipe_memo_hit = 45 551` / `miss = 142`, `preflight_mtlb_unloadable` and `preflight_air_unextractable` both **0**. The pre-scan reaches *more* work, not less: `preflight_memo_ready` 47 577 → **54 142** on comparable `_absent` (46 540 → 46 568), which is the continuations and indirect dispatches the byte scan was not reaching. And the cost the plan's census called "the part most obviously redundant" is **gone rather than smaller**: every `preflight_split` line reads `refs_us=0 refs_n=0`, against `refs_us=7 refs_n=47` per second on the baseline — nothing charges that part any more, because there is no second decode of the stream to charge. That reading is what licenses removing `PreflightPart::Refs` in this commit rather than leaving an always-zero column that reads as "this part is free". **What this boot does not establish**: the compute loss fell 2 → 1, not to 0, and one boot cannot separate a real halving from the boot-to-boot variance the journal already records at about one boot in three. The same boot also shows one *render*-side `linux_m2v_draw … stage=fragment pipeline_ref=5` with `model_pipeline=ready` — a draw refused against a fragment shader still translating while the ordering plane held the pipeline ready — which is the same defect shape on the arm `94838490` was supposed to have closed, and is owed a diagnosis. The log also carries one `observe_slug_collision` for this very slug, claimed by both `runtime::m2v_cache::M2vCacheDecline` and `backend::vulkan::engine::draw_prepare`, so a bare `grep -c` of it conflates two holders; the counts above are by `stage=`. |
| *(no register row — `f696867d`, `77f5bd88` then `e2d11cb1`, the pipeline that stopped being ready)* | `vm/boot-x86.sh --device reims-vgpu-pci --interactive --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five apps) | The last of the lost draws this device could explain, and the model was wrong about its own state rather than the pre-scan being wrong about the packet. Four boots put every remaining `m2v_translation_pending_at_sync_boundary` in one shape: `linux_m2v_async queued pipe=5 stage=Fragment air=4332` → `done` → `pipeline_lease_ready site=…_pump slot=5`, then twenty seconds later `queued pipe=5 … air=4328` with the refused draw four milliseconds inside that window, and the compute one identical at `pipe=14 air=11764 → 11760`. **The guest rewrites the shader behind a live pipeline ref in place** — `pipeline_retired = 4` against 143 declared, `pipe_memo_miss = 138`, no delete, so no generation and the same `ResourceId`. `f696867d` measured the population at the one place both identities are in one hand: **five `m2v_pipe_content_changed` a desktop**, which is what says the name-keyed pre-scan memo is worth keeping (`pipe_memo_hit = 44 832`) and only its key is wrong. The pre-scan was never the defect: it read the new AIR, queued the translation and answered pending — and `PipelineState::Ready` had no step back except `Retired`, so it meant "was translated once", `waits_for` returned nothing, and the ordering plane released the transaction anyway. `77f5bd88` gave the table the step and `PipelineTable::withdraw` as its own door; `e2d11cb1` made `preflight_translations` return the refs it could not serve instead of a bool, because a bool cannot tell the lease that must stop being ready from the lease that must become it. **The door is separate because an existing test said so**: `a_rail_walks_a_declared_pipeline_to_ready_and_a_second_walk_is_declined` failed the moment `advance` allowed the step, since the Metal rail retains no pipeline state and re-walks `Translating → Compiling → Ready` on every draw — through `advance`, every draw would un-ready its own pipeline. Validation boot: **every translation-pending loss is gone** — no `m2v_translation_pending_at_sync_boundary` and no `draw_prepare_{vertex,fragment}_translate` anywhere in the log, against one to four on each of the four boots before it — with **0 `FAIL` lines**, `packet_unadmitted = 0`, `rail_selected = 1` and `observe_slug_collision = 0`. **Nothing is stranded**: the table's final occupancy is `pending=0 declared=0 translating=0 compiling=0 ready=139 refused=0 retired=4 total=143`, and `pipeline_lease_withdrawn = 7` against `pipeline_ready = 150` = `declared 143` + those same 7 — every withdrawal came back. The withdrawal is not a loop: 7 against 150 readies and 257 611 render records. The two draws still lost are `engine_drew_store_lost_it`, the unimplemented MSAA writeback (300×300 `samples=4`), which is unrelated and unchanged. **The cost, stated:** a withdrawn lease stops the pump's `parked_translations_already_ready` short-circuit for its position, so `preflight_memo_absent` roughly doubles (46 568 → 97 923) — about 420 ms of guest AIR resolves across a 220-second boot, or 1.9 ms/s. `stamp_publish_behind = 14 534` here and 14 141 on the pre-change baseline: **pre-existing on this tree and not this change**, but it contradicts the zero an earlier row above records, and that row's claim is the one to re-establish rather than this number to explain. |
| *(second rail — `f696867d` .. `e2d11cb1`, the withdrawal on the rail that deletes pipelines)* | `vm/boot-x86.sh --device reims-vgpu-pci --interactive --rail macos-26` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload | The withdrawal on the guest that provokes it five times harder, chosen because it is the rail every earlier row's defect was found on and because the new step is a *scheduling* change whose failure mode is a hang. It does not hang and it strands nothing: **0 `FAIL` lines** across 11 MB, `rail_selected = 1`, and the table rests at `pending=0 declared=0 translating=0 compiling=0 ready=108 refused=0 retired=120 total=228`. `m2v_pipe_content_changed = 26` here against macos-15's 5 — this guest rewrites shaders far more often — and `pipeline_lease_withdrawn = 14` over twelve distinct slots, with `pipeline_ready = 242` = `declared 228` + those 14 exactly, the same identity the macos-15 row records. Fourteen withdrawals against 535 026 render records is not the Metal-rail cycle. **The defect the change was for is closed on this rail too, and the residue is a different one, which the `model_pipeline=` field added by `d1637062` is what says.** Five losses remain — two render, three compute — and **not one of them reads `model_pipeline=ready`**. Both render ones read **`model_pipeline=retired`** (`pipe=72`, `pipe=29`): the guest deleted the pipeline and a packet recorded before the delete still binds it, which is exactly the loss `6587f618` chose to take when it stopped refusing the whole packet for a deleted pipeline — *"one draw is what the guest loses, which is what it lost before this table existed"* — and this rail retires **120** pipelines a boot against macos-15's 4. The three compute ones carry no `model_pipeline` field and are **owed a diagnosis**; the render arm's field is what made its two legible, and the compute refusal has no equivalent. `packet_unadmitted = 9` over two latched reasons, `lifecycle_unknown_ref` and `resolve_ref_names_no_object` — the guest defect an earlier row records at 1 on a differently-shaped run; **no macos-26 boot was taken at the pre-change commit, so that number is unattributed rather than clean.** Nothing in this change touches admission's refusal path: a withdrawn lease is `Translating`, which `waits_for` answers `Pending` for, and a refusal is only ever `Lease::Refused` or absent. |
| *(no register row — this commit, the two counters a boot was needed to read)* | `vm/boot-x86.sh --device reims-vgpu-pci --interactive --rail macos-26` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five applications over ssh) | **Two open questions closed by one boot, and neither had a code change in it.** First, the ordering group's classification denominator. The `9ab40542`/`fb76145e` row's macos-15 reading was `packet_class_unclassified = 2166`, every one of them child `0x28` `CmdDeleteObject`, whose ledger row was then `Closure::Unresolved`. That row is `Closure::Implemented` now, and this rail sent nothing else `reims_vgpu_core::transaction::classify` refuses: **`packet_class_exec=946`, `packet_class_lifecycle=1096`, `packet_class_present=172`, `packet_class_query=3`, `packet_class_control=10` — 2227 classified — and `packet_class_unclassified=0`**, with no `packet_class_unclassified` line on the failure channel. The four rows still unresolved (`CmdDebug`, `CmdDisplaySleepState`, `CmdDisplaySetProperties`, `CmdDelay`) are a contract question this guest does not exercise. Second, `stamp_publish_behind`, which `48f6252e` instrumented precisely because a bare count could not say whether the meaning had drifted or the invariant had broken — this row's own predecessor recorded it at **zero** and every boot since `2099769c` reads about fourteen thousand. **The meaning drifted.** Three slots answered it, all inside the boot's first 24 seconds and none after, by one to seven: `slot=1 page=1 held=3`, `slot=2 page=19 held=26`, `slot=4 page=0 held=1`. The zero was taken when the drain's page read was the plane's only publisher; `SessionModel::complete` publishes the channel's released stamp straight into the scheduler now, and the word itself is queued on the GPU-ordered rail — `stamp_visible_observed = 1 700 804` against `stamp_visible_inline = 25`. The page is behind because the write has not landed, not because it went back: `stamp_write_forward = 2203` against `stamp_write_backward = 1`. **And nothing is decided inside the window**: `stamp_wait_model_ahead = 0` and `stamp_wait_model_behind = 0` against 767 waits decided (`stamp_wait_model_agrees_met = 405`, `stamp_wait_model_agrees_unmet = 362`), which is the whole difference between this and the `stamp_wait_model_ahead = 12 824` the publication point was moved to escape. The rest of the boot holds: `m2v_translation_pending_at_sync_boundary` absent, `observe_slug_collision = 0`, `packet_unadmitted = 2`, `pipeline_lease_withdrawn = 77`, `pipeline_refused = 0`. What it also names, and this row is where it is written down rather than acted on: `preflight_mtlb_unloadable = 4` and **two** draws refused `draw_prepare_pipeline_missing` with `model_pipeline=ready` beside them. The pre-scan cannot load an absent pipeline's shader containers, so it answers "no translation is pending" — which is true — and admission readies the lease on that answer. `Ready` is nonetheless the right lease: a refused lease refuses the *whole* exec packet at admission, so ending it would trade one lost draw for every other draw in every packet binding the ref, and a `PipelineMissing` at this rate is transient. No test moved with this row; nothing moved. |
| *(no register row — this commit, the frame half of the reply destination)* | `vm/boot-x86.sh --device reims-vgpu-pci --interactive --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five applications over ssh) | The macos-15 half of the row above, and two of the plan's named-unsettled terms read on both rails. **The classification denominator, retaken on the rail it was written against**: `packet_class_exec=9571`, `packet_class_lifecycle=15033`, `packet_class_present=1066`, `packet_class_query=13`, `packet_class_control=11` — 25 694 classified — and `packet_class_unclassified=0` against the 2166 the `9ab40542`/`fb76145e` row recorded, with `packet_unadmitted=0` and no `FAIL` line. Both rails now refuse nothing for want of a payload class. **The reply-destination term, task-GVA half**: `query_reply_scanned=24` and `query_reply_outside_every_allocation=24` here, 4 and 4 on macos-26 — twenty-eight replies, none inside an allocation this device identifies, no `query_reply_inside_an_allocation` line on either rail. So a task-GVA reply destination given a backing of its own is the construction these guests support. **The frame half was unreadable and now is not.** Both rails read `device_info_reply_scanned=1` and `device_info_reply_after_an_identity=1` — the guest asks after this device has minted, every time, so the mint-count arm never closes on a real rail. It was also answering the wrong question: a monotone never-reused counter feeds all three key spaces, so a fresh number cannot equal one already handed out by construction, while the danger the plan names is *aliasing* — the reply page being storage some other identity already covers. This commit asks that of geometry, in the one key space a page frame can collide with: whether the reply frame is one of a live mapping's `page_entries`. New routes `device_info_reply_frame_in_no_mapping` (closing) and `device_info_reply_frame_in_a_mapping` (open), reachable on a rail that has already minted. **Also read, and still unrecovered**: `backing_id_heap_placed=0` on macos-15 and macos-26 both, so no driven boot has placed a heap texture and the heap-extent term stays refused by name — macos-26 was one of the four rails the plan lists as untried and is now tried. Replacement coverage for the new arm: fn `runtime::objects::tests::a_device_info_reply_frame_is_measured_against_the_mappings_this_device_holds`, which pins the live-mapping hit, the unmapped-mapping miss and the never-named-page miss. No test moved with this row; nothing moved. |
| *(no register row — the six-rail sweep for `7ddd6f72`/`02bd23cd`, no code change)* | `vm/rail-sweep.sh` over **all six** provisioned x86 rails — macos-11, macos-12, macos-13, macos-14, macos-15, macos-26 — each `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail <r>` (x86 macOS guest, Linux/KVM host, Vulkan rail) driven by the committed workload, one binary shared by all six (13:26–14:02) | **The reply-destination term closed on every rail this project can boot, and nothing else moved.** Every rail reached first frame and completed the workload; `FAIL` appears in **none** of the six logs (11: 15 952 lines, 12: 9 817, 13: 14 421, 14: 20 399, 15: 20 839, 26: 10 511). **`device_info_reply_frame_in_a_mapping = 0` on all six** and `device_info_reply_frame_in_no_mapping` fires on all six, so the frame the guest is told to write its device-info reply into is never one of a live mapping's pages — the aliasing arm the geometry question was asked to find does not exist on any supported guest, and the term is settled rather than merely untried. `device_info_reply_after_an_identity = 1` and `device_info_reply_before_any_identity = 0` everywhere, which is why the earlier mint-count arm could never close. **`packet_class_unclassified = 0` on all six**, against the 2166 the plan's superseded section recorded — the ingress classification has no residue on any rail. `stamp_wait_model_ahead = 0` and `delete_unnamed_but_list_still_has_it = 0` on all six. `exec_segment_unended`, `stream_frame_fail`, `stream_record_fail`, `exec_compute_record_unopened` and `query_reply_inside_an_allocation`: **zero on all six**, established by key absence and not by summing per-interval dumps. Query replies: 24 / 28 / 38 / 42 / 24 / 4 scanned across the six, **156 in total and every one outside every allocation this device identifies**, which is the task-GVA half of the same term on six rails instead of two. Render traffic spans two orders of magnitude across the rails (`walk_records_render` 13 041 on macos-12 to 230 110 on macos-15) and `packet_class_exec` 574 to 7 635, so the zeros above are not one workload's accident. **Still unrecovered, on all six**: `backing_id_heap_placed = 0` — no driven guest on any provisioned rail places a heap texture, so the heap-extent term stays refused by name and the four rails the plan listed as untried are now tried. No test moved with this row; nothing moved. Replacement coverage is unchanged from the two rows above, which this sweep re-runs on four more rails. |
| *(no register row — this commit, the object caches leave the process-global engine)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five applications over ssh) | **The first handle-owning subsystem out of the process-global engine, on a live guest.** `EngineState::caches` is gone; `ObjectCaches` now lives in the device's own `RailDeviceState` slot as `VulkanDeviceState::caches`, and `execute_draw_request`, `execute_compute_request`, `flush_batched_draws` and `object_cache_levels` take the `&DeviceState` they reach it through. Guest reached the desktop and completed the full workload; **`FAIL` absent from all 23 172 log lines**, `vk_device_recreate` absent, and `device_lost=0` on every one of the 212 `engine_delta` lines. `object_cache_levels` — the line that reads the moved caches, now through the device — climbs and settles exactly as before (`shaders=4 layouts=2 passes=1 pipelines=2 samplers=1` at t=29 s to `shaders=140 layouts=62 passes=21 pipelines=318 samplers=19 compute_pipelines=10` at t=242 s), so the caches are populated, read and warm on the device's slot. **`vk_engine_device_caches_unreachable` never fires**, which is the refusal the new arm names for a slot another rail holds — unreachable under `backend::select`'s one-rail-per-process latch, and now measured to be. Replacement coverage: fn `vk_engine_parity::a_second_devices_caches_are_its_own_and_ending_one_spares_the_other`, the assertion the process-global shape could not express — a second `DeviceState` on the *same* `VkDevice` draws the identical request cold (`creates > 0`, inheriting none of the first device's warm pipelines), goes warm on its own second draw (`creates == 0`), and its ending leaves the first device's levels unchanged and its next draw still warm. No test moved with this row; nothing moved to `dead/`. |
| *(no register row — `b37b6740`, the window's second registry dependency)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five applications over ssh) | Recorded here because it corrects the *size* of a cutover step rather than taking one, and because the correction was found by reading production and confirmed by a boot. Removing `begin_present`'s registry re-resolve — the read that holds the resident registry in the process-global engine — was sized as "publish carries what the window reads off the slot". It is not: the window reads `slot.access` as the **current Vulkan layout and source masks**, feeds it to its blit barrier's `oldLayout`/`srcStageMask`/`srcAccessMask`, and writes back `registry_note_access(TransferRead)` afterwards. That is a read-modify-write, and a barrier from an access the image has left is an invalid transition rather than a stale picture. `WINDOW_SOURCE_EPOCH` answers only the first of the two questions. So `b37b6740` carries `ResolvedResident` beside the stamp and *compares* it against the re-resolve — five named classes, first difference wins, both resolutions in the line — while the blit still uses the re-resolve, so no frame changes. **The boot: all five classes absent, zero `FAIL` lines, 136 cadence windows all `busy=0` and 89 at `direct_frac=1.00`, `slate_source_stale` absent, `walk_records_render` 278 859 and 169 515 draws.** The publish-time resolution and the window's own re-resolve agreed on every presented frame — the reading the removal needs, on the presenting half. **What it does not say:** `evicts=0` and `gen_mismatch=0` again, so no published resident *left* the registry during the boot and the removal half of the epoch's cover is still unmeasured. That is the same gap the previous three boots left and it is the one thing still owed before the re-resolve goes. Replacement coverage: seven owner-level tests on `divergence_class` as the total function it is — one per class, one that the coarsest difference wins when every field moved at once, one that no two classes share a slug, one that every rendered field is a whitespace-free `k=v` pair, one that a vanished source names only what was promised. No test moved with this row; nothing moved to `dead/`. |
| *(no register row — `d6155cf1`, the window's licence moves to the type that owns the registry)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five applications over ssh), then a clean SIGTERM teardown | Recorded here because it moved a rule rather than a file, and because the comment it replaced said the rule could not be moved. `invalidate_all_resident_holders` bumped two counters and its own doc conceded that "a *new* wholesale-invalidation site that bumps only one counter is exactly the defect, and no test relates a future site to this function". `pools::WINDOW_SOURCE_EPOCH` now moves inside `ResourcePools` at the three points a published resident's promise can end — `unregister_resident` (the sole `registry.remove`), `destroy_all` (the sole `registry.drain`, bumped on the line *above* the loop that frees the images), and `Drop for ResourcePools` (the `*pools = ResourcePools::new()` path that has no `ctx` to destroy anything through). `invalidate_all_resident_holders` keeps only `RESIDENT_RESOURCE_EPOCH`, which has no such owner. A wholesale site that forgets to call it can no longer strand a window stamp. **The boot:** zero `FAIL` lines, all five `window_source_*` divergence classes still absent, 122 cadence windows all `busy=0` with 97 at `direct_frac=1.00`, `walk_records_render` 264 964 and 155 742 draws — indistinguishable from `b37b6740`'s boot on the presenting path, which is the claim, since this changes only what happens when a registry goes away. The teardown the SIGTERM drives is the `destroy_all` this commit added a bump to: QEMU exited on signal 15, the harness reverted its clone, and the fail channel stayed at zero across all 24 005 lines. Replacement coverage: fn `the_resident_registry_has_exactly_one_removal_and_one_drain` (a source-level count over all of `pools/`, since `registry` is a private field and a third exit added later is a use-after-free nothing in the build would relate to this rule), fn `the_wholesale_bump_precedes_the_destruction_it_answers_for`, and two `Drop` tests — one that a pools carrying a window source moves the stamp when it goes, one that a pools carrying none moves nothing, because every test in this crate builds one. No test moved with this row; nothing moved to `dead/`. |
| *(no register row — `1fd17c08`, the promised access)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five applications over ssh), then a clean SIGTERM teardown | The window's *second* registry dependency, closed, and the boot that says the closing is free. `ResidentAccess` is the `oldLayout`, `srcStageMask` and `srcAccessMask` of the barrier the present blit builds, so a resolution whose image is right and whose access is stale is an invalid transition rather than a torn frame — and `WINDOW_SOURCE_EPOCH` vouched only for the image. `ResourcePools::set_registry_access` is now the sole writer of a resident's access and moves the epoch when the value **changes** under a published identity. On a change and not on a write, because a colour target rendered again ends in the access it was already in and a per-write bump would send every present through host memory. **The cost, which the earlier census could not bound** — it compares publish-time against present-time and cannot see a change that reverts in between, and the window's own blit write-back is exactly such a change, once per direct present. **The boot: 123 cadence windows all `busy=0`, 95 at `direct_frac=1.00`** against 97 and 89 on the two boots before it, `slate_source_stale` absent, all five `window_source_*` classes still absent, zero `FAIL` across 23 822 lines, `walk_records_render` 275 915 and 171 675 draws. The rule fires and never reaches the window: the publish that follows a bump takes its stamp after it, and the only source that can go stale is one being re-presented without a new frame. Replacement coverage: fn `moving_a_published_residents_access_moves_the_window_stamp`, fn `rewriting_the_access_a_published_resident_already_holds_moves_nothing` (eight times over — the frame-cost rule), fn `moving_an_unpublished_residents_access_moves_nothing`, and fn `the_registry_writes_a_residents_access_in_exactly_one_place`, a source-level count over all of `pools/` taking any receiver rather than a binding named `slot`. No test moved with this row; nothing moved to `dead/`. |
| *(no register row — `a9a7d515`, the window reads no registry)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (three rounds of launching and quitting five applications over ssh), then a clean SIGTERM teardown | **The host window's present path no longer touches the resident registry.** `begin_present`'s two `registry_get` calls are gone and with them the presenter's whole judgement of a resident: it takes the `WindowPresentResolution` the publish produced under the engine lock and checks one atomic stamp. This is what four commits of this session were for — the window thread can never hold a `&DeviceState` (the lock one comes behind is the drain's, held for a whole render tranche at 935-979 ms per exec packet), so the registry's move to the device needed the window to stop reading it rather than to read it more cheaply. Six `SlateReason` variants become two: `no_resident`, `content_not_ready`, `not_bgra` and `geom_mismatch` were the presenter judging what it had re-resolved, and are now decided by `resident_present_decision` a frame earlier and named there as `winpub_*` on the drain's route channel. **The boot: zero `FAIL` across 21 806 lines, 99 cadence windows all `busy=0`, 73 at `direct_frac=1.00` — every window past the 25 firmware ones and the one crossover, which is the same shape as the 97/123 and 97/122 the two boots before it read** — `slate_source_stale` absent, no `winpub_*` route fired at all, first guest frame presented via rail resident (same-device zero-copy), `walk_records_render` 275 577 and 176 642 draws, clean SIGTERM teardown. Replacement coverage for the four presenter-side classification tests that moved out from under: fn `the_publish_names_the_first_blocker_a_resident_carries` (the same ladder at its owner, plus the every-rung-failing case saying the coarsest wins), fn `each_publish_blocker_has_its_own_route_name` (each rung's `winpub_*` name, and that the accepting arm returns exactly `slot_window_resolution` of the slot it accepted at the epoch it read) and fn `a_declined_publish_vouches_for_nothing`. The seven divergence-census tests went with the census, which after this has nothing to compare. Nothing moved to `dead/`. |
| *(no register row — `13f9aa49`, the blit refusal carries its own reason)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (`vm/drive-desktop.sh`, three rounds of launching and quitting five applications over ssh), then a clean SIGTERM teardown | Seam 6's disconnect list names `BLIT_FAIL_REASON` — "a thread-local carrying a reason the observation channel should carry" — and this is that item, closed. The rail's 177 checks collapse into six classes, so the specific cause had to travel somewhere; it travelled in a thread-local that `br` set, every command entry cleared, and `Refusal::refusal` read back on three unchecked assumptions (same thread, before any other write, at all). It also said nothing about the nineteen sites that never called `br` — `.ok_or(BlitStatus::Capacity)?` and `return Err(BlitStatus::GuestIo)` in the buffer↔texture aspect helpers — which reached the dispatch line as `blit_unattributed`. `BlitStatus` splits into `BlitFailure` (the six classes a caller branches on) and `BlitStatus::Failed { failure, reason }`, `br` is its only constructor, and a refusal with no reason is now unspellable; the nineteen did not compile until each named the check it had been discarding. **The boot: zero `FAIL` across 27 263 lines, 167 cadence windows all `busy=0` with 139 at `direct_frac=1.00`** — the 26 firmware windows with no guest frame plus two crossover windows (0.83, 0.90) account for the rest, the same shape as the 73-of-99 and 97-of-123 the boots before it read — `walk_records_render` 301 624 over 166 windows, 129 678 draws, first guest frame presented via rail resident, clean SIGTERM teardown and revert. **No blit refusal fired at all**, which is the honest reading of this boot: it says the change introduced none, not that the nineteen new slugs were exercised — a driven desktop does not refuse blits, and the nineteen are covered at their owner instead. One `slate_source_stale` episode, one frame, at `display_enable_mask 0xd -> 0xc`: the documented lawful case (the epoch moved between the publish's stamp and the present), reported here because the three rows above record it as absent and this boot is 167 windows against their 99. Replacement coverage: fn `a_refusal_cannot_be_spelled_without_the_check_that_fired` (which asserts the construction rather than the old `blit_unattributed` name, because there is no site left that can produce it), fn `a_refusal_names_which_of_the_collapsed_checks_fired`, and eleven copy/fill-executor tests that read the channel after the status now assert both in one `BlitStatus::Failed { .. }` literal — a stronger statement than the pair was, since it says the class and the check came from the same refusal, which is exactly what the thread-local could not promise. Nothing moved to `dead/`. |
| *(no register row — `e1a4af18` and `9b2470e0`, the publish submits what it vouches for)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (`vm/drive-desktop.sh`, three rounds of launching and quitting five applications over ssh), then a clean SIGTERM teardown | The presenter's `pools.batch_flush` moves to `prepare_window_resident_present`. The flush had to be somewhere — `content_ready` is set when a draw *records* into a resident, so a resolution can name pixels that exist only in a command buffer nobody has ended — and the presenter was the wrong somewhere twice over: the batch is the drain's and the publish *is* the drain, so the window thread was submitting a whole tranche of guest work on the display's latency path; and a flush at present time also submits every draw recorded *after* the publish, so the frame reaching the screen was not the frame the publish vouched for. A flush failure declines by name (`winpub_batch_flush_failed`) and latches `note_device_lost_seen`, because this transaction holds the engine guard and no `ObjectCaches` and that order is fixed. **The boot: zero `FAIL` across 22 868 lines, 112 cadence windows all `busy=0` with 86 at `direct_frac=1.00`** — 24 firmware windows and two crossovers (0.83, 0.88) are the rest, so 86 of the 88 windows past the firmware phase, the same shape as the 139-of-167 the row above read — `walk_records_render` 291 960 over 111 windows, 127 344 draws, first guest frame presented via rail resident, clean SIGTERM teardown and revert. One `slate_source_stale` episode again. **The window thread's engine lock, which is the reading this change is actually about: median 4 acquisitions per second, median 155 µs held per second, median longest single hold 68 µs.** One outlier window at 902 ms total with a single 554 ms hold, which is the swapchain creation at startup and is named here so a later reader does not take it for a steady-state number. That is the floor the remaining work has to beat, and it is small — what is left in that hold is the blit recording and one registry write. `9b2470e0` is the doc that closes `00a52e81`'s open question about that write: it cannot move to the publish (which would promise a transition a `Busy` present never makes, and the lie is the *access*, not the layout, since a primary target and `transfer_read(false)` share `TRANSFER_SRC_OPTIMAL`) and it cannot be replaced by a restore after the blit (the layout comes back, the dependency does not — a guest colour write barriering from the restored `ColorWrite` names `COLOR_ATTACHMENT_OUTPUT` and does not wait for this blit's `TRANSFER` read). The barrier and the record of it are one action; the write leaves when the present becomes a transaction against the device that owns the registry. Replacement coverage: fn `the_window_publish_flushes_the_open_batch_before_it_vouches`, a device test whose assertion is ahead of the publish's own accept/decline, so a refactor that moved the flush behind the decision cannot pass on the accepting arm alone. Nothing moved to `dead/`. |
| *(no register row — `3e4276c5` and `8aa30037`, the live warm draw stops allocating)* | `vm/boot-x86.sh --device reims-vgpu-pci --testing --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail), host-driven workload (`vm/drive-desktop.sh`, three rounds of launching and quitting five applications over ssh), then a clean SIGTERM teardown | "Heap allocations per steady-state draw" is a required value in the plan's structural-zero table. `reims-vgpu-core` had met it for the semantic model; nothing measured the live executor. Measured, a warm repeat draw took **1 trip, 16 bytes**, every run — all of it `exec::clear_values` returning a `Vec` whose first `push` allocated, on the overwhelmingly common draw of one colour target with no secondaries and no depth. It is a `ClearValues` array now and the answer is zero. Capacity is the wire's own bound (`1 + MAX_SECONDARY_ATTACH` is `PASS_MAX_COLOR_ATTACHMENTS`) plus depth, and overflow refuses under the same `SecondaryAttachmentCap` reason the pass-key path uses rather than truncating, because Vulkan indexes the array positionally and a short one gives a later attachment an earlier one's colour. **The boot, which is what a change on the render-pass begin path owes: zero `FAIL` across 25 213 lines, 146 cadence windows all `busy=0` with 119 at `direct_frac=1.00`** — 25 firmware windows and two crossovers (0.83, 0.91) are the rest, so 119 of the 121 past the firmware phase, the same shape as the 86-of-88 and 139-of-141 the two rows above read — `walk_records_render` 285 753 over 146 windows, 123 510 draws, first guest frame presented via rail resident, clean SIGTERM teardown and revert. `slate_source_stale` absent this time. **What the gate does not yet cover, said here rather than left for a reader to assume:** the measured shape binds no descriptors, and `Vec::with_capacity(0)` allocates nothing, so the zero is a statement about descriptor-free draws. A draw that binds anything still builds a `Vec<BindingSig>` and clones it into the pipeline key — at least two trips — and that is the next piece of the same gate. Replacement coverage: fn `a_warm_repeat_draw_does_not_enter_the_allocator` (which brackets a counter snapshot, asserting one `batch_joins` and no `batch_flushes` inside the measured region, because an early return allocates nothing too and would pass for the wrong reason) and fn `a_clear_array_past_the_attachment_bound_refuses_rather_than_truncating`. Nothing moved to `dead/`. |
| *(no register row — `191b06c9`, the binding-table split)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail) | The boot the descriptor change owed, because it moves what a draw hands `vkCmdPushDescriptorSetKHR`. Guest reached the desktop through the resident zero-copy path (`first guest frame presented via rail resident`), host-driven workload (three rounds of launching and quitting five applications over ssh), clean SIGTERM teardown, **zero `FAIL` across 19 776 log lines**. The positive evidence is the split's own census: **153 187 draws**, `descriptor_pushes=34 423` against `descriptor_push_held=9 018` — so 9 018 draws found the command buffer already carrying their bindings and, since this commit, build no Vulkan write structures at all, where before they built and dropped a `Vec<vk::DescriptorBufferInfo>` and a `Vec<vk::WriteDescriptorSet>` each. `descriptor_set_updates=3` and `descriptor_set_binds=3` in the whole boot, which is the reading that says the allocated-set arm is the exception and the push rail is the one worth measuring. `pass_hits=55 141`, `pipeline_hits=43 089`, `layout_hits=43 380`, `vertex_buffer_bind_slots=144 943`, `batch_joins=41 432` — every one of them a value that reached the encoder through the rewritten path. |
| *(no register row — `5471721f`, the three per-draw lists)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail) | Owed because it moves where a draw's vertex and storage binds live and replaces the gather-role table with a scan. Guest reached the desktop, host-driven workload, clean SIGTERM teardown, **zero `FAIL` across 25 173 log lines**. **172 885 draws**, `vertex_buffer_bind_slots=162 246` reaching the encoder and `vertex_buffer_bind_emitted` equal to it — the bind list now lives in the command buffer's scratch, and those two being equal is what says nothing was dropped moving it — resolved into `vertex_buffer_bind_calls=47 576` consecutive runs. `buffer_bind_reuses=303 021`, `descriptor_pushes=38 795` against `descriptor_push_held=10 374`, `pipeline_hits=48 745`, `pass_hits=61 948`. The gather-role scan replaced a table whose three call sites `expect()` a classification: `buffer_guest_imports=158 028` binds went through them without one returning `None`, which is the whole of what the table was consulted for. **What this boot does not cover:** `buffer_guest_gathers=0` and `buffer_gather_declined=0` — this host imports every guest buffer window directly, so the role's *byte columns* (`buffer_guest_gather_{vertex,storage,shared,index}_bytes`) were never written on it and the scan's split is asserted only by `buffer_gather_roles_partition_physical_content_allocations`. A host without pointer import is what would exercise them. |
| *(no register row — `155fe27b` and `158888b3`, layout and attribute-set interning)* | `vm/boot-x86.sh --testing --device reims-vgpu-pci --rail macos-15` (x86 macOS guest, Linux/KVM host, Vulkan rail) | Owed because both change what a draw's pipeline key *is*. Guest reached the desktop, host-driven workload, clean SIGTERM teardown, **zero `FAIL` across 23 735 log lines**, **172 862 draws**. The reading that is about this change is `attr_sets=14`: fourteen distinct vertex-attribute sets for a whole boot, flat across every window. That is the plateau the unbounded object caches are justified by, measured on the newest of them, and it is why the level was added to `object_cache_levels` rather than left uncounted. Beside it the interned layouts answered `layout_hits=48 880` against `layout_misses=59`, `pipeline_hits=48 610`/`pipeline_misses=329`, `pass_hits=61 828`/`pass_misses=20`, `shader_hits=97 637`/`shader_misses=142` — the "no cache miss for a live unchanged contract object after first resolution" zero, as close to met as a boot that compiles new shaders can show. `descriptor_pushes=39 254` against `descriptor_push_held=9 680`, `vertex_buffer_bind_slots=161 833` equal to `vertex_buffer_bind_emitted`. |
